# Core AnyChain Agent runtime dependencies.
#
# Install in an isolated virtual environment. Do not install into the host
# Python used by blockchain nodes.
#
# The filename is retained as a compatibility alias. Google ADK is deliberately
# not a core dependency: the LangGraph Harness makes OpenAI-compatible HTTP
# calls for DeepSeek, OpenAI, Claude, and Gemini on Vertex. Install the optional
# Gemini google_search extra with:
#
#   bash scripts/install_agent_deps.sh --yes --with-google-search
#
# Python 3.10+ is required for the Agent runtime.

langgraph
langgraph-checkpoint-sqlite
openai
prompt-toolkit
PyYAML
cryptography
