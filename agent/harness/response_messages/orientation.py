"""Orientation response definitions."""

MESSAGES = {
    "harness.orientation.greeting": {
        "en": "Hi, I am AnyChain Benchmark Agent. State a test goal, ask about current state, or paste configuration, logs, or reports.",
        "zh": "你好，我是 AnyChain Benchmark Agent。你可以直接说明测试目标、询问当前状态，或粘贴配置、日志和报告。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.clarify_unresolved": {
        "en": "To avoid applying only part of this turn, clarify the following items that were not mapped safely:\n{details}",
        "zh": "为了避免只应用你这一轮的部分需求，请先澄清以下尚未安全映射的内容：\n{details}",
        "arguments": {"details": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.session_reset": {
        "en": "Cleared the previous Agent configuration. Startup discovery and historical jobs were preserved. Tell me what to test.",
        "zh": "已清空之前的 Agent 配置。启动环境推断和历史 job 已保留。请告诉我这次要测试什么。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.identity": {
        "en": "I am AnyChain Benchmark Agent, running in the current AnyChain Benchmark project. I configure, validate, execute, and analyze blockchain-node benchmarks and reports. State a goal, ask about current state, or paste configuration, logs, or reports.",
        "zh": "我是 AnyChain Benchmark Agent，运行在当前 AnyChain Benchmark 工程中，负责区块链节点测试的配置、校验、执行和报告分析。你可以直接提出测试目标、询问当前状态，或粘贴配置、日志和报告。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.capabilities": {
        "en": "I am AnyChain Benchmark Agent. I support fake-node closed-loop validation, real-node RPC load tests, sync-observe, custom RPC methods, new-chain onboarding, preflight/smoke, job tracking, and log/report analysis.\nCurrent framework facts: {chain_count} chains, {family_count} adapter families, {method_count} RPC methods.",
        "zh": "我是 AnyChain Benchmark Agent。我支持 fake-node 闭环验证、real-node RPC 压测、sync-observe 同步观察、自定义 RPC、新链协议适配、preflight/smoke、job 跟踪，以及日志和报告分析。\n当前框架事实：{chain_count} chains, {family_count} adapter families, {method_count} RPC methods.",
        "arguments": {
            "chain_count": "string",
            "family_count": "string",
            "method_count": "string",
        },
        "kinds": {"message"},
    },
    "harness.orientation.consultation.extension": {
        "en": "There are three extension cases: custom RPC for a supported chain, a new chain in an existing adapter family, and secondary development for an unsupported family. Custom RPC flow for supported chain `{chain}`: provide the endpoint selected for real validation, then the method name, params/request, response, or official documentation. The Agent extracts and validates the schema, confirms whether defaults are retained, augmented, or replaced, then confirms single/mixed mode and weights totaling 100. Template defaults: {defaults}. Runtime configuration never mutates `config/chains` defaults.",
        "zh": "扩展分三类：已支持链的自定义 RPC、现有协议族的新链，以及未支持协议族的二次开发。已支持链 `{chain}` 的自定义 RPC 流程：提供用于真实验证的 endpoint，再提供 method 名、params/request、response 或官方文档。Agent 会抽取并校验 schema，让你确认是保留、追加还是替换默认 methods，然后确认 single/mixed 和总和为 100 的权重。模板默认 methods：{defaults}。运行时配置不会修改 `config/chains` 默认模板。",
        "arguments": {"chain": "string", "defaults": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.supported_chains": {
        "en": "Configured chains: {chains}. Ask for one chain to see its default RPC workload.",
        "zh": "已配置链：{chains}。你可以指定一条链，查看它的默认 RPC workload。",
        "arguments": {"chains": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.supported_chain": {
        "en": "Default RPC workload for `{chain}`:\n- single: {single}\n- mixed: {mixed}\n- methods: {methods}",
        "zh": "链 `{chain}` 的默认 RPC workload：\n- single: {single}\n- mixed: {mixed}\n- methods: {methods}",
        "arguments": {
            "chain": "string",
            "single": "string",
            "mixed": "string",
            "methods": "string",
        },
        "kinds": {"message"},
    },
    "harness.orientation.consultation.requirements": {
        "en": "Before testing, the Agent confirms: 1. test type and chain; 2. cloud and machine (`CLOUD_REGION`, `CLOUD_ZONE`, `MACHINE_TYPE`); 3. Ledger/data disk, optional accounts/state disk, and network; 4. endpoint and node process; 5. RPC workload, custom methods, weights, and fixtures; 6. QPS profile; 7. observability and advanced settings; 8. preflight/smoke, jobs, and reports. Users may jump or go back at any time; the next missing group is recomputed from state. real-node requires `LOCAL_RPC_URL`; sync-observe requires a real-node source and does not configure RPC workload/QPS or run Vegeta.",
        "zh": "测试前会逐组确认：1. 测试类型和链；2. 云和机器（`CLOUD_REGION`、`CLOUD_ZONE`、`MACHINE_TYPE`）；3. Ledger/data、可选 accounts/state 磁盘和网络；4. endpoint 和节点进程；5. RPC workload、自定义 method、权重和 fixtures；6. QPS profile；7. 可观测性和高级配置；8. preflight/smoke、job 和报告。用户可以随时跳转或回退，完成后从状态重新计算下一个缺失项。real-node 需要 `LOCAL_RPC_URL`；sync-observe 需要真实节点来源，不配置 RPC workload/QPS，也不走 Vegeta。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.requirements_with_recommendation": {
        "en": "Before testing, the Agent confirms: 1. test type and chain; 2. cloud and machine (`CLOUD_REGION`, `CLOUD_ZONE`, `MACHINE_TYPE`); 3. Ledger/data disk, optional accounts/state disk, and network; 4. endpoint and node process; 5. RPC workload, custom methods, weights, and fixtures; 6. QPS profile; 7. observability and advanced settings; 8. preflight/smoke, jobs, and reports. Users may jump or go back at any time; the next missing group is recomputed from state. real-node requires `LOCAL_RPC_URL`; sync-observe requires a real-node source and does not configure RPC workload/QPS or run Vegeta.\nFor the lowest-cost end-to-end toolchain check, start with fake-node smoke. It needs no real endpoint but does not represent real-node performance.",
        "zh": "测试前会逐组确认：1. 测试类型和链；2. 云和机器（`CLOUD_REGION`、`CLOUD_ZONE`、`MACHINE_TYPE`）；3. Ledger/data、可选 accounts/state 磁盘和网络；4. endpoint 和节点进程；5. RPC workload、自定义 method、权重和 fixtures；6. QPS profile；7. 可观测性和高级配置；8. preflight/smoke、job 和报告。用户可以随时跳转或回退，完成后从状态重新计算下一个缺失项。real-node 需要 `LOCAL_RPC_URL`；sync-observe 需要真实节点来源，不配置 RPC workload/QPS，也不走 Vegeta。\n如果目标只是低成本确认整个工具链能否闭环，先选择 fake-node smoke；它不需要真实 endpoint，但不能代表真实节点性能。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.mode_comparison": {
        "en": "fake-node only validates the framework loop with fixtures; it does not represent real-node performance or answer real-node QPS capacity. real-node benchmark uses `LOCAL_RPC_URL` to load a real endpoint for QPS, latency, and bottleneck analysis. sync-observe watches real-node catch-up, process CPU/memory, disk, network, and client-native MGas/s when available, without Vegeta.",
        "zh": "fake-node 的作用是使用 fixtures 验证框架闭环，不代表真实节点性能，也不能回答真实节点支持多少 QPS；real-node benchmark 使用 `LOCAL_RPC_URL` 对真实 endpoint 执行 RPC 压测，用于 QPS、延迟和瓶颈分析；sync-observe 观察真实节点追块、进程 CPU/内存、磁盘、网络及客户端可用的 MGas/s，不走 Vegeta。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.performance_guidance": {
        "en": "fake-node only validates the framework loop with fixtures; it does not represent real-node performance or answer real-node QPS capacity. real-node benchmark uses `LOCAL_RPC_URL` to load a real endpoint for QPS, latency, and bottleneck analysis. sync-observe watches real-node catch-up, process CPU/memory, disk, network, and client-native MGas/s when available, without Vegeta.\nTherefore, choose real-node benchmark when the goal is QPS capacity and bottleneck discovery.",
        "zh": "fake-node 的作用是使用 fixtures 验证框架闭环，不代表真实节点性能，也不能回答真实节点支持多少 QPS；real-node benchmark 使用 `LOCAL_RPC_URL` 对真实 endpoint 执行 RPC 压测，用于 QPS、延迟和瓶颈分析；sync-observe 观察真实节点追块、进程 CPU/内存、磁盘、网络及客户端可用的 MGas/s，不走 Vegeta。\n因此，如果目标是测试可支持的 QPS 和瓶颈，应选择 real-node benchmark。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.preflight_smoke": {
        "en": "Preflight validates dependencies, configuration, endpoints, and execution prerequisites. Smoke runs the complete path at safe low traffic to verify requests, logs, artifacts, and reports. Smoke is not a production performance conclusion.",
        "zh": "preflight（预检）在执行前校验依赖、配置、endpoint 和运行前置条件；smoke（冒烟测试）以安全的小流量执行完整链路，验证请求、日志、产物和报告能够闭环。smoke 不是正式性能结论。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.recommendation": {
        "en": "For a quick end-to-end Agent/toolchain check, start with fake-node smoke. It needs no real node but does not represent real-node performance. Start with `solana`, or name the target chain.",
        "zh": "如果只想快速确认 Agent 和工具链能否闭环，建议先用 fake-node smoke；它不需要真实节点，但不代表真实节点性能。可以从 `solana` 开始，也可以直接说出目标链。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.reset_help": {
        "en": "You can clear the current workflow and start over, or keep confirmed values and modify any configuration group. Reset preserves startup discovery and job history. Say `clear configuration` or name the item to change.",
        "zh": "可以完全清空当前配置并重新开始，也可以保留已确认值并修改任意配置组。重置不会删除启动环境推断和历史 job。请直接说明“清空配置”或要修改的项目。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.evidence_help": {
        "en": "Yes. Paste logs, a stack trace, or command output; multiline input is collected as one evidence block. You may also name a job id so the Agent can read its existing logs and artifacts.",
        "zh": "可以。粘贴日志、错误栈或命令输出；多行内容会作为一个证据块接收。也可以指定 job id，让 Agent 读取已有日志和产物后分析。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.clarification": {
        "en": "The previous answer did not resolve the question. Continue with the original question; the Agent will answer from current state without treating it as configuration confirmation or advancing the workflow.",
        "zh": "刚才的回答没有解决你的问题。请继续原问题；Agent 会结合当前状态回答，不会把它当作配置确认或推进流程。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.new_chain_endpoint": {
        "en": "This validates whether a non-template chain can use an existing adapter family. Provide a reachable HTTP RPC endpoint, at least one RPC method with params/response samples, and official docs when available. The validation endpoint is evidence only and does not automatically become the final benchmark endpoint.",
        "zh": "这里是在验证未内置的新链能否使用现有协议族。请提供可访问的 HTTP RPC endpoint、至少一个 RPC method 及参数/响应示例；官方文档也可以作为证据。该 endpoint 只作为验证证据，不会自动成为最终被测 endpoint。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.custom_rpc_endpoint": {
        "en": "This validates a custom RPC method. Provide a reachable validation endpoint plus method, params, response samples, or official docs. Enabled mixed-workload weights must total 100. The validation endpoint does not replace `LOCAL_RPC_URL` or modify the chain template.",
        "zh": "这里是在验证自定义 RPC method。请提供可访问的验证 endpoint，以及 method、params、response 示例或官方文档。mixed workload 的启用 method 权重最终必须合计 100。验证 endpoint 不会覆盖 `LOCAL_RPC_URL`，也不会修改原始 chain template。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.custom_rpc_fixture_choice": {
        "en": "Fixture validation is ready for custom RPC methods: {methods}. Existing confirmed configuration remains preserved: {preserved_fields}. Choose whether to record fixtures now or provide more RPC evidence first.",
        "zh": "以下自定义 RPC method 已进入 fixture 验证阶段：{methods}。现有已确认配置会继续保留：{preserved_fields}。请选择现在录制 fixture，或先补充更多 RPC 证据。",
        "arguments": {"methods": "string", "preserved_fields": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.current_config": {
        "en": "Current configuration:\n- target mode: {target_mode}\n- workflow mode: {workflow_mode}\n- chain: {chain}\n- RPC mode: {rpc_mode}\n- QPS mode: {qps_mode}\n- observability: {observability}\n- confirmed fields: {confirmed_fields}\n- pending question: {pending_question}\n- pending field: {pending_field}\nRuntime selections and custom RPC overrides are job-local; the original config/chains template is unchanged.",
        "zh": "当前配置：\n- target mode: {target_mode}\n- workflow mode: {workflow_mode}\n- chain: {chain}\n- RPC mode: {rpc_mode}\n- QPS mode: {qps_mode}\n- observability: {observability}\n- 已确认字段：{confirmed_fields}\n- 待确认问题：{pending_question}\n- 待确认字段：{pending_field}\n运行时选择和自定义 RPC 覆盖只对当前 job 生效；原始 config/chains 模板不会被修改。",
        "arguments": {
            "target_mode": "string",
            "workflow_mode": "string",
            "chain": "string",
            "rpc_mode": "string",
            "qps_mode": "string",
            "observability": "string",
            "confirmed_fields": "string",
            "pending_question": "string",
            "pending_field": "string",
        },
        "kinds": {"message"},
    },
    "harness.orientation.consultation.workload_effective": {
        "en": "Effective RPC workload: chain `{chain}`, mode `{rpc_mode}`, methods: {methods}, mixed weights: {weights}.",
        "zh": "当前有效 RPC workload：链 `{chain}`，模式 `{rpc_mode}`，methods：{methods}，mixed 权重：{weights}。",
        "arguments": {"chain": "string", "rpc_mode": "string", "methods": "string", "weights": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.workload_template": {
        "en": "Template workload for `{chain}`: default single method `{single}`; default mixed weights: {mixed}. These values are not confirmed and the original chain template is unchanged.",
        "zh": "链 `{chain}` 的模板 workload：single 默认 method 为 `{single}`；mixed 默认权重为 {mixed}。这些值尚未确认，原始 chain template 不会被修改。",
        "arguments": {"chain": "string", "single": "string", "mixed": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.rpc_mode": {
        "en": "`single` sends one selected RPC method throughout the workload. `mixed` sends several enabled RPC methods according to explicit weights; enabled weights must total 100. After choosing a mode, you can keep the chain-template defaults or validate custom methods and configure the effective workload.",
        "zh": "`single` 在整个 workload 中只发送一个已选 RPC method。`mixed` 按明确权重发送多个已启用 RPC methods，启用权重必须合计 100。选择模式后，可以保留 chain template 默认值，也可以验证自定义 methods 并配置当前 workload。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.current_context_pending": {
        "en": "Current workflow context: active group `{active_group}`; pending question `{pending_question}` (field `{pending_field}`); execution status `{execution_status}`; saved evidence lines: {evidence_lines}.\nNext, answer the pending question shown below or state which configuration group to change.",
        "zh": "当前 workflow 状态：active group `{active_group}`；待确认问题 `{pending_question}`（字段 `{pending_field}`）；执行状态 `{execution_status}`；已保存证据行数：{evidence_lines}。\n下一步请回答下方待确认问题，或直接说明要修改哪个配置组。",
        "arguments": {
            "active_group": "string",
            "pending_question": "string",
            "pending_field": "string",
            "execution_status": "string",
            "evidence_lines": "integer",
        },
        "kinds": {"message"},
    },
    "harness.orientation.consultation.current_context_next_group": {
        "en": "Current workflow context: active group `{active_group}`; no question is pending; execution status `{execution_status}`; saved evidence lines: {evidence_lines}.\nThe next incomplete configuration group is `{next_group}`.",
        "zh": "当前 workflow 状态：active group `{active_group}`；当前没有待确认问题；执行状态 `{execution_status}`；已保存证据行数：{evidence_lines}。\n下一个未完成的配置组是 `{next_group}`。",
        "arguments": {
            "active_group": "string",
            "execution_status": "string",
            "next_group": "string",
            "evidence_lines": "integer",
        },
        "kinds": {"message"},
    },
    "harness.orientation.consultation.current_context_preflight": {
        "en": "Configuration collection is complete. The next step is preflight/smoke validation. Confirm whether to run it; you may also state a configuration group to change first.",
        "zh": "配置收集已完成。下一步是运行 preflight/smoke 验证。请确认是否执行；也可以先说明要修改哪个配置组。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.current_context_evidence": {
        "en": "Evidence collection is active. Saved evidence lines: {evidence_lines}. Redacted preview: {preview}\nContinue pasting evidence, finish the collection, or state which workflow question to answer first.",
        "zh": "当前正在收集证据。已保存证据行数：{evidence_lines}。脱敏预览：{preview}\n你可以继续粘贴证据、结束收集，或说明要先回答哪个 workflow 问题。",
        "arguments": {
            "evidence_lines": "integer",
            "preview": "string",
        },
        "kinds": {"message"},
    },
    "harness.orientation.consultation.chain_research_ungrounded": {
        "en": "Chain identity research for `{subject}`: exists={exists}; canonical name `{chain}`; protocol/API `{protocol}`; supported adapter family `{adapter_family}`; confidence `{confidence}`. Evidence: {evidence}. Internet verification was not performed. This is consultation evidence only and does not change the selected chain.",
        "zh": "对 `{subject}` 的链身份查询结果：是否存在={exists}；规范名称 `{chain}`；协议/API `{protocol}`；已支持 adapter family `{adapter_family}`；置信度 `{confidence}`。证据：{evidence}。未进行互联网核实。这只是咨询证据，不会修改当前已选链。",
        "arguments": {
            "subject": "string",
            "exists": "string",
            "chain": "string",
            "protocol": "string",
            "adapter_family": "string",
            "confidence": "string",
            "evidence": "string",
        },
        "kinds": {"message"},
    },
    "harness.orientation.consultation.chain_research_grounded": {
        "en": "Grounded chain identity research for `{subject}`: exists={exists}; canonical name `{chain}`; protocol/API `{protocol}`; supported adapter family `{adapter_family}`; confidence `{confidence}`. Evidence: {evidence}. Internet evidence: {grounding}. This is consultation evidence only and does not change the selected chain.",
        "zh": "对 `{subject}` 的链身份互联网核实结果：是否存在={exists}；规范名称 `{chain}`；协议/API `{protocol}`；已支持 adapter family `{adapter_family}`；置信度 `{confidence}`。证据：{evidence}。互联网证据：{grounding}。这只是咨询证据，不会修改当前已选链。",
        "arguments": {
            "subject": "string",
            "exists": "string",
            "chain": "string",
            "protocol": "string",
            "adapter_family": "string",
            "confidence": "string",
            "evidence": "string",
            "grounding": "string",
        },
        "kinds": {"message"},
    },
    "harness.orientation.consultation.environment_snapshot_ready": {
        "en": "Environment status: ready.\n- cloud: {cloud}\n- deployment: {deployment}\n- region: {region}\n- zone: {zone}\n- machine: {machine}\n- CPU: {cpu}\n- memory: {memory}\n- network candidate: {network}\n- ledger candidate: {ledger}\n- accounts candidate: {accounts}\n- missing required dependencies: {missing_required}\n- missing optional dependencies: {missing_optional}",
        "zh": "环境状态：就绪。\n- cloud: {cloud}\n- deployment: {deployment}\n- region: {region}\n- zone: {zone}\n- machine: {machine}\n- CPU: {cpu}\n- memory: {memory}\n- network 候选：{network}\n- ledger 候选：{ledger}\n- accounts 候选：{accounts}\n- 缺失的必需依赖：{missing_required}\n- 缺失的可选依赖：{missing_optional}",
        "arguments": {"cloud": "string", "deployment": "string", "region": "string", "zone": "string", "machine": "string", "cpu": "string", "memory": "string", "network": "string", "ledger": "string", "accounts": "string", "missing_required": "string", "missing_optional": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.environment_snapshot_blocked": {
        "en": "Environment status: blocked by missing required dependencies.\n- cloud: {cloud}\n- deployment: {deployment}\n- region: {region}\n- zone: {zone}\n- machine: {machine}\n- CPU: {cpu}\n- memory: {memory}\n- network candidate: {network}\n- ledger candidate: {ledger}\n- accounts candidate: {accounts}\n- missing required dependencies: {missing_required}\n- missing optional dependencies: {missing_optional}",
        "zh": "环境状态：被缺失的必需依赖阻塞。\n- cloud: {cloud}\n- deployment: {deployment}\n- region: {region}\n- zone: {zone}\n- machine: {machine}\n- CPU: {cpu}\n- memory: {memory}\n- network 候选：{network}\n- ledger 候选：{ledger}\n- accounts 候选：{accounts}\n- 缺失的必需依赖：{missing_required}\n- 缺失的可选依赖：{missing_optional}",
        "arguments": {"cloud": "string", "deployment": "string", "region": "string", "zone": "string", "machine": "string", "cpu": "string", "memory": "string", "network": "string", "ledger": "string", "accounts": "string", "missing_required": "string", "missing_optional": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.config_field_required": {
        "en": "`{field}` ({label})\nPurpose: {purpose}\nContract: required; applies to {modes}. Accept a detected candidate when available; otherwise enter it manually.",
        "zh": "`{field}`（{label}）\n作用：{purpose}\n属性：必填；适用模式：{modes}。如果检测到候选值，可以确认该值；否则需要手动输入。",
        "arguments": {"field": "string", "label": "string", "purpose": "string", "modes": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.config_field_optional": {
        "en": "`{field}` ({label})\nPurpose: {purpose}\nContract: optional; applies to {modes}. Accept a detected candidate when available; otherwise enter it manually.",
        "zh": "`{field}`（{label}）\n作用：{purpose}\n属性：可选；适用模式：{modes}。如果检测到候选值，可以确认该值；否则需要手动输入。",
        "arguments": {"field": "string", "label": "string", "purpose": "string", "modes": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.job_none": {
        "en": "No historical job is available.",
        "zh": "当前没有历史 job。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.job_verified": {
        "en": "Current job: `{job_id}`, verified status: `{status}`.",
        "zh": "当前 job：`{job_id}`，已验证状态：`{status}`。",
        "arguments": {"job_id": "string", "status": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.job_unverified": {
        "en": "The live status of job `{job_id}` cannot be verified; the last-known status is `{status}`. Check whether its artifacts remain accessible.",
        "zh": "无法验证 job `{job_id}` 的实时状态；最后记录状态为 `{status}`。请检查该 job 的产物是否仍可访问。",
        "arguments": {"job_id": "string", "status": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.consultation.general": {
        "en": "I can explain product capabilities, test modes, requirements, current state, historical jobs, errors, and reports, or continue the current setup. Ask the question directly.",
        "zh": "我可以解释产品能力、测试模式、配置要求、当前状态、历史 job、错误日志和报告，也可以继续当前配置流程。请直接说明问题。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.quarantine_inspection": {
        "en": "Quarantine reason: {reason}. Safely identified confirmed values: {safe_values}. Choose retain-safe or clear-and-restart.",
        "zh": "隔离原因：{reason}。可安全识别的确认值：{safe_values}。请选择保留安全值或清空重新开始。",
        "arguments": {"reason": "string", "safe_values": "string"},
        "kinds": {"warning"},
    },
    "harness.orientation.safe_values_retained": {
        "en": "Retained only safely identified confirmed values. Say what to continue or modify.",
        "zh": "仅保留了可安全识别的确认值。请说明要继续或修改什么。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.session_resumed": {
        "en": "Continuing the previous configuration.",
        "zh": "已继续之前的配置。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.recommendation_declined": {
        "en": "OK, not using the recommendation.",
        "zh": "好的，不按推荐。",
        "arguments": {},
        "kinds": {"message"},
    },
    "harness.orientation.recommendation_accepted": {
        "en": "Starting the recommended `{mode}` path.",
        "zh": "好的，按推荐进入 `{mode}`。",
        "arguments": {"mode": "string"},
        "kinds": {"message"},
    },
    "harness.orientation.failure.invalid_language": {
        "en": "Response language must be zh or en.",
        "zh": "响应语言必须是 zh 或 en。",
        "arguments": {},
        "kinds": {"error"},
    },
    "harness.orientation.failure.unresolved_clauses_required": {
        "en": "At least one unresolved clause is required for clarification.",
        "zh": "澄清操作至少需要一个尚未解析的子句。",
        "arguments": {},
        "kinds": {"error"},
    },
    "harness.orientation.failure.answer_pending_not_resolved": {
        "en": "The pending answer must be resolved against the active question contract.",
        "zh": "待确认回答必须根据当前问题契约解析。",
        "arguments": {},
        "kinds": {"error"},
    },
    "harness.orientation.failure.no_safe_action": {
        "en": "No safe typed action was resolved.",
        "zh": "没有解析出可安全执行的类型化动作。",
        "arguments": {},
        "kinds": {"error"},
    },
    "harness.orientation.failure.unsupported_action": {
        "en": "The orientation owner does not support action `{action_type}`.",
        "zh": "orientation owner 不支持动作 `{action_type}`。",
        "arguments": {"action_type": "string"},
        "kinds": {"error"},
    },
    "harness.orientation.failure.unsupported_question": {
        "en": "The orientation owner does not support question `{question_id}`.",
        "zh": "orientation owner 不支持问题 `{question_id}`。",
        "arguments": {"question_id": "string"},
        "kinds": {"error"},
    },
}
