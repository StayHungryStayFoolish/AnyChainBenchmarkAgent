# AnyChain Agent External Validation Record — EXT-20260813-006

After confirming (Y-Y-Y) a custom-RPC-method schema (`eth_getBlockByNumber`
with params `["0x1b4", true]`), selecting `N` (or `2`) to "Probe
method against saved validation endpoint now?" returns the user to
the SAME "Provide schema evidence for eth_getBlockByNumber" prompt
they already answered — with no escape hatch. The user cannot skip
the probe AND move on to the next configuration field. This forces
an infinite Y-Y-Y-N confirmation loop. Observed in `WW-CASE1-001`
turns 8-18; reproduced twice in the same session before turn budget
exhaustion.

## 身份

- Record ID: `EXT-20260813-006`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Tracked worktree clean: `yes`
- Runtime: Ubuntu Docker `blockchain-node-benchmark:test-ubuntu`
  container `bench-preflight`
- Host: Debian 13 gLinux GCE VM, x86_64

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
- External user AI/model: Claude Sonnet 4.6 (Vertex `us-east5` rawPredict)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`

## 起始状态

- Manifest scenario ID: `WW-CASE1-001`
- Journey/task ID: fresh cold-start
- Session/checkpoint ID: fresh langgraph state
- Starting groups and states: mid-flow after custom-RPC schema
  confirmation (Y-Y-Y at turns 9, 10, 11)
- Pending question ID/owner at turn 11: `Probe method ... against
  the saved validation endpoint now? ... Y authorizes only this
  probe; N returns to evidence correction.`
- Chain case: `supported` (ethereum)
- Prior job ID/status: none

## 精确观察

- Previous complete agent response for turn 11 (contract confirmation):
  ```
  Agent> Confirm the extracted RPC request contract and reviewed parameter semantics.
  Evidence: jsonrpc_request; transport: jsonrpc
  Method: `eth_getBlockByNumber`
  Parameters: blockNumber: json_type=string, semantic_type=block_number, encoding=hex, ...; fullTransactionObjects: json_type=boolean, ...
  ...
  1. Y
  2. N
  ```
- Exact Sonnet turn 11 input: `Y` → agent proceeded to:
  ```
  Agent> Probe method `eth_getBlockByNumber` against the saved validation endpoint now? Prior probe: not-run; contract status: request-confirmed-response-pending. `Y` authorizes only this probe; `N` returns to evidence correction.
  1. Y
  2. N
  ```
- Turn 12 input: `N` → agent responded:
  ```
  Agent> Provide schema evidence for `eth_getBlockByNumber`: params JSON, a curl/request, a response sample, or an official-document excerpt. Use `[]` when there are no parameters.
  Enter a value.
  ```
- **This is the exact same prompt already answered at turn 8 with
  `["0x1b4", true]`.** The N branch drops the just-confirmed
  contract and re-solicits evidence with no visible progress markers.
- Reproduced at turn 18: same `Y-Y-Y-N` sequence with `2` (numeric
  N alias) → same loop back to "Provide schema evidence".
- Attempted escape hatches also rejected:
  - Turn 13 `done` → generic clarify template
  - Turn 14 `skip` → generic clarify template
  - Turn 19 `done` → generic clarify template
- Tool receipt path + SHA-256: none — control-flow decision
- Runtime event/checkpoint path + SHA-256: `langgraph-snapshot.tgz`
  sha256=`a73a41805352c388f418444737ff37f5c8e60dc81ef9b339276f1080832745de`
- Session state path + SHA-256: `session-state.json`
  sha256=`43b0f0535ebc9c1a2841bf04d793ad494d0d7a97fc7f49acc8f30a63fcfcbc20`
- Driver log path + SHA-256: `driver.jsonl`
  sha256=`ffb16a94088f83b87cc3cecaa8a7102aa9d6208e763aa9d9d0cb9e012c224cc5`
- First failing turn index: turn 12
- Expected contract/postcondition: after Y-Y-Y on schema + params +
  contract, the "Probe now? Y/N" prompt is offering an optional
  action. `N` should preserve the confirmed contract AND proceed to
  the next stage (either "confirm workload" or "next chain field"),
  not roll back to the "provide evidence" prompt. If N is intended to
  mean "start over with different evidence", the prompt should say
  that explicitly ("`N` = discard this contract and re-enter
  evidence").
- Observed behavior: N silently rolls back to "provide evidence" with
  no message explaining WHY the previously-confirmed contract was
  discarded. User has no way to continue without re-probing.
- User impact: users who want to accept a schema WITHOUT running a
  live probe (e.g., offline development, rate-limited endpoint) hit
  an infinite loop. The prompt's own text says "`N` returns to
  evidence correction" but "correction" is ambiguous — the user
  doesn't know if their evidence was WRONG or if this is just the
  next stage.
- Reproduction count: 2 (turns 12 and 18-19)
- Fresh isolated session reproduced: yes

## 分类

- Classification: `product_failure`
- Suspected root owner: `domain:custom-rpc-flow` (probe-Y/N branch
  handling) OR `orchestration` (flow state after probe declined)
- Supporting evidence: the loop reproduces deterministically across
  two full Y-Y-Y-N cycles in the same session; no alternate escape
  hatch works (`done`, `skip` both rejected)
- Evidence against stale session/data contamination: fresh session
- Not tested because: (i) whether declining the probe AND declining
  weight adjustment (the natural "just skip" combo) works; (ii)
  whether an explicit `continue`, `next`, `save`, or `accept without
  probe` keyword would escape

## 安全传输

- Redaction review completed by: Claude Sonnet 4.6 (recorder)
- Bundle path: `.agent/external-validation/a22108e53401cf7d9314917b870b71ccf338619c/transcripts/WW-CASE1-001/`
- Bundle SHA-256: per-file hashes in updated SHA256SUMS
- Internal `SHA256SUMS` verified on personal workstation: `no`
- Missing evidence: none

## 个人电脑 Codex 确定性修复

- Evidence bundle integrity verified: pending Codex
- Reproduced on exact source revision: pending Codex
- Root cause: pending Codex — hypothesis: the probe-N branch's
  "return to evidence correction" transition drops the confirmed
  contract without a "confirmed-without-probe" terminal state
- Violated architecture authority: pending Codex — likely the
  custom-RPC customization contract expects "probe optional, contract
  final on user confirm"
- Single authoritative repair owner: pending Codex —
  `domain:custom-rpc-flow` most likely
- Framework-level repair design: pending Codex — after Y-Y-Y contract
  confirm, N to probe should transition to the NEXT flow stage
  (workload summary / add-another / continue-to-benchmark). If
  starting-over is intended, add an explicit "Start over with new
  evidence" option alongside "Continue without probe".
- Files changed: pending Codex
- Repair revision: pending Codex
- Focused deterministic tests: Y-Y-Y-N flow reaches next stage;
  Y-Y-Y-Y flow runs probe and reaches next stage; explicit "continue
  without probe" keyword works if added
- Adjacent regression tests: Y-N-* short circuits still work
  correctly at the earlier param confirmations; probe-Y branch
  actually probes
- Complete Docker/Linux gates: pending Codex
- Why this is not a phrase/keyword patch: the fix must add an
  explicit "confirmed-without-probe" terminal transition, not a
  string-match on `done`/`skip`

## 工作电脑回验

- Clean checkout of repair revision: pending
- Original failed journey result: pending
- Adjacent variant 1: attempt `continue` at the probe-N loop — pending
- Adjacent variant 2: attempt to add second custom method after N — pending
- Gemini/ADC/google_search result: pending
- New evidence bundle + SHA-256: pending
- Remaining gap: pending
- Closure decision and reason: pending
