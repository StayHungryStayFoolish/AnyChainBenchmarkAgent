# AnyChain Agent External Validation Record — EXT-20260808-008

Input normalization does not strip decorative punctuation before intent
matching; `??? benchmark solana ???` fails to match the well-documented
`benchmark solana` meta-command and is instead fragmented into two
clarification-bucket items.

## 身份

- Record ID: `EXT-20260808-008`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Runtime: Ubuntu 24.04 Docker `blockchain-node-benchmark:test-ubuntu`
- Host: Debian 13 gLinux VM
- Tracked worktree clean: `yes`

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
- External user AI/model: Claude Sonnet 4.6
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`

## 起始状态

- Manifest scenario ID: `WW-LANGUAGE-001`
- Journey/task ID: same session as EXT-20260808-007; after turn 3
  ("使用 solana") successfully bound chain=solana
- Session/checkpoint ID: chain bound to solana as of turn 3
- Chain case: `supported` (solana in registry)

## 精确观察

- Previous complete agent response path + SHA-256:
  `.agent/external-validation/a22108e5.../transcripts/WW-LANGUAGE-001/session.log`
  sha256=`15e6ba1b8232876ce69823cb25165868b872fa2cdb13aa67d06f63df8e36
  d2f9` (turn 3 response confirming chain=solana with Chinese menu)
- Exact Claude user input path + SHA-256: same session.log,
  input string `??? benchmark solana ???` (turn 4)
- Next complete agent response path + SHA-256: same session.log,
  output text:
  ```
  Agent> [thinking] Processing the current request. Press Ctrl+C to cancel this turn without exiting the Agent.
  Agent> To avoid applying only part of this turn, clarify the following items that were not mapped safely:
  - ???
  - benchmark solana ???
  I am AnyChain Benchmark Agent. What would you like help with? State a test goal or choose the next step.
  [4-option orientation menu in English]
  ```
- Runtime event/checkpoint path + SHA-256: langgraph-snapshot.tgz
  sha256=`82adaee2d01dd9641b1f1e9ccd8bb2cfbaf12998dca2d42aa27f24596cff
  791c`
- First failing turn index: turn 4
- Expected contract/postcondition: agent's startup hint (session.log
  line matching "Try: benchmark solana, use fake-node, doctor, jobs,
  status, logs <job_id>, follow <job_id>, help, exit.") advertises
  `benchmark solana` as a valid meta-command. Standard input
  normalization should strip leading/trailing punctuation before
  intent matching, so `??? benchmark solana ???`, `benchmark
  solana?`, `"benchmark solana"`, and similar decorated forms all
  resolve to the same intent as `benchmark solana`.
- Observed behavior: input tokenizer splits `??? benchmark solana ???`
  on some delimiter (possibly on `?` characters or whitespace) into
  two fragments `???` and `benchmark solana ???`, both of which fail
  to match a registered intent and are enumerated in the clarification
  bucket. The command `benchmark solana` embedded within is not
  extracted.
- User impact: users typing conversationally (with question marks,
  exclamation points, quotes) get clarification-bucket rejections
  for otherwise-valid commands. Compounded with the ~45-80 second
  latency of clarification-bucket responses (observed across turns
  1-4 of this scenario, related to EXT-20260808-006 pattern), each
  such rejection costs the user close to a minute. Users cannot
  reliably invoke advertised meta-commands unless they type them
  exactly as documented.
- Reproduction count: 1
- Fresh isolated session reproduced: `no` (pending Codex)

## 分类

- Classification: `product_failure`
- Suspected root owner: `admission` (primary — input normalization
  step responsible for stripping decorative punctuation before
  handing to intent classification) OR `semantic_planning`
  (secondary — intent classifier fragmenting on punctuation instead
  of running on normalized input)
- Supporting evidence: the SPLIT into `???` and `benchmark solana ???`
  reveals a tokenizer that is punctuation-aware but keeps punctuation
  in fragments; a normalization step could exist but is not
  configured to strip leading/trailing punctuation
- Evidence against stale session/data contamination: input arrived
  fresh in turn 4; agent process pid stable; chain state (solana)
  was already bound so command should short-circuit to a benchmark
  start flow
- Not tested because: (i) other punctuation forms not tested (single
  `?`, `!!!`, quotes, brackets, ellipsis); (ii) leading-only or
  trailing-only decoration not tested; (iii) mixed content
  ("please benchmark solana") not tested (natural-language wrapping
  vs decorative punctuation are different but often confounded)

## 个人电脑 Codex 确定性修复

- Root cause: (Codex — hypothesis: admission layer lacks a
  normalize-punctuation-before-intent-match step; or the normalizer
  exists but does not run for the input path that reaches intent
  fragmentation)
- Single authoritative repair owner: `admission`
- Framework-level repair design: (Codex — add a normalization step
  that strips leading/trailing decorative punctuation before intent
  match; unit-test with a matrix of punctuation forms; do NOT patch
  intent classifier to accept punctuated forms, since that leaks
  cleanup responsibility across boundaries)
- Focused deterministic tests: (Codex — matrix of punctuation
  forms × known meta-commands; verify all decorated forms match the
  same intent as bare form)
- Adjacent regression tests: (Codex — pure-punctuation input still
  produces clarification bucket; punctuation within a word like
  "l'assistant" is preserved)

## 工作电脑回验

- Original failed journey result: pending
- Adjacent variant 1: `benchmark solana?` — pending
- Adjacent variant 2: `"benchmark solana"` — pending
