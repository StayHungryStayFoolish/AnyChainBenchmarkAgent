# AnyChain Agent External Validation Record — EXT-20260808-007

Explicit locale-lock request (e.g., "从现在开始，请只用中文和我对话") is
dropped into the clarification-bucket protocol instead of updating
session locale as a system-level directive.

## 身份

- Record ID: `EXT-20260808-007`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Runtime: Ubuntu 24.04 Docker `blockchain-node-benchmark:test-ubuntu`
- Host: Debian 13 gLinux VM
- Tracked worktree clean: `yes`

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
- External user AI/model: Claude Sonnet 4.6 (this recorder)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`

## 起始状态

- Manifest scenario ID: `WW-LANGUAGE-001`
- Journey/task ID: fresh `.agent/` state (prior `.agent/terminal` +
  `.agent/langgraph` removed before session start)
- Session locale at turn: en (default from fresh startup)
- Prior turn: turn 1 "y" → clarification-bucket rejection with English
  response

## 精确观察

- Previous complete agent response path + SHA-256:
  `.agent/external-validation/a22108e5.../transcripts/WW-LANGUAGE-001/session.log`
  sha256=`15e6ba1b8232876ce69823cb25165868b872fa2cdb13aa67d06f63df8e36
  d2f9` (the English clarification response from turn 1 ending with
  the English orientation menu)
- Exact Claude user input path + SHA-256: same session.log,
  input string `从现在开始，请只用中文和我对话` (turn 2)
- Next complete agent response path + SHA-256: same session.log,
  output text:
  ```
  Agent> [thinking] 正在处理当前请求。按 Ctrl+C 可取消本轮，不会退出 Agent。
  Agent> 为了避免只应用你这一轮的部分需求，请先澄清以下尚未安全映射的内容：
  - 从现在开始，请只用中文和我对话
  我是 AnyChain Benchmark Agent。你想让我帮你做什么？请直接说明测试目标，或选择下一步。
  [4-option orientation menu in Chinese]
  ```
- Runtime event/checkpoint path + SHA-256: langgraph-snapshot.tgz
  sha256=`82adaee2d01dd9641b1f1e9ccd8bb2cfbaf12998dca2d42aa27f24596cff
  791c`
- Session state path + SHA-256: session-state.json sha256=
  `43b0f0535ebc9c1a2841bf04d793ad494d0d7a97fc7f49acc8f30a63fcfcbc20`
- First failing turn index: turn 2
- Expected contract/postcondition: an explicit locale-directive input
  should be recognized as a system-level command that updates
  `session_state.language` and confirms the change with a
  locale-appropriate acknowledgement (e.g., "好的，我会用中文回答后续
  问题。" for zh-lock, "Understood, I will respond in English going
  forward." for en-lock). This is a common natural-language
  interaction pattern that users expect to work.
- Observed behavior: the input is treated as an unmappable "safely-
  mapped" fragment and enumerated in the clarification bucket. Agent
  auto-detected the input as Chinese (evidence: response was rendered
  in Chinese), so a locale-detection path exists — but it does not
  connect to a locale-directive intent handler. Response Chinese
  locale confirms the per-turn detection is what's driving locale,
  not any recognition of the user's explicit lock request.
- User impact: users cannot pin the agent's response language via
  natural language. Compounded with EXT-20260808-003 (per-turn locale
  bypass of session state), a user in a mixed-language environment
  has no user-accessible mechanism to enforce a stable response
  locale for evaluation, transcription, or accessibility purposes.
- Reproduction count: 1

## 分类

- Classification: `product_failure`
- Suspected root owner: `semantic_planning` (primary — intent
  classifier does not have a locale-directive intent registered) OR
  `admission` (secondary — locale-directive utterances are filtered
  or normalized away before reaching intent classification)
- Supporting evidence: input was correctly detected as Chinese for
  response-locale purposes (proving language-detection layer works);
  but was not routed to a session-state-mutation intent
- Not tested because: (i) English-form request "please respond in
  Chinese only" not tested; (ii) short-form request "用英文" not
  tested; (iii) whether an explicit slash-command form (e.g.,
  `/lang zh`) exists is not tested

## 个人电脑 Codex 确定性修复

- Root cause: (Codex — hypothesis: no locale-directive intent
  registered in `agent/semantic_planning/`; language-change is a
  side effect of input-locale detection only, never a first-class
  user-controllable state transition)
- Single authoritative repair owner: `semantic_planning` (register
  intent) + `response_authority` (act on updated session locale;
  requires resolution of EXT-20260808-003 too)
- Framework-level repair design: (Codex — register locale-directive
  intent with acknowledgement response; write session_state.language;
  ensure response formatter reads session locale)
- Focused deterministic tests: (Codex — zh/en/mixed locale-lock
  utterances; confirmation response in target locale; subsequent
  responses honor the lock)

## 工作电脑回验

- Original failed journey result: pending
- Adjacent variant 1: English locale-lock "please respond in Chinese
  only" — pending
- Adjacent variant 2: short-form "用中文" — pending
