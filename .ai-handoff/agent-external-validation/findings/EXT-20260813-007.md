# AnyChain Agent External Validation Record — EXT-20260813-007

At the "Provide official protocol/RPC documentation, endpoint
documentation, or a request and/or response example" prompt in the
unsupported-adapter handoff flow, chatty prose that DOES contain a
valid JSON request example is rejected wholesale with the generic
"clarify unmapped" template. The evidence extractor does not parse
the JSON fragment out of surrounding prose. This blocks the
unsupported-adapter handoff pathway from actually collecting evidence
when the user does not have a copy-pastable pure-JSON document.

## 身份

- Record ID: `EXT-20260813-007`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Tracked worktree clean: `yes`
- Runtime: Ubuntu Docker `blockchain-node-benchmark:test-ubuntu`
  container `bench-preflight`
- Host: Debian 13 gLinux GCE VM, x86_64

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
- External user AI/model: Claude Sonnet 4.6 (Vertex `us-east5` rawPredict)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`

## 起始状态

- Manifest scenario ID: `WW-CASE3-001`
- Journey/task ID: fresh cold-start
- Session/checkpoint ID: fresh langgraph state
- Starting groups and states: orientation menu at start
- Pending question ID/owner at turn 4: `Provide official protocol/RPC
  documentation, endpoint documentation, or a request and/or response
  example.` (unsupported-adapter handoff evidence collection)
- Chain case: `unsupported_adapter` (`foochain`)
- Prior job ID/status: none

## 精确观察

- Previous complete agent response for turn 4:
  ```
  Agent> This chain is outside the supported adapter families. Provide
  official protocol/RPC docs, endpoint docs, and request/response
  examples; I will generate a secondary-development handoff.
  Provide official protocol/RPC documentation, endpoint documentation,
  or a request and/or response example. You may submit the part
  currently available.
  Enter a value.
  ```
- Exact Sonnet user input for turn 4 (from `driver.jsonl`
  sha256=`c51074f922c590594209a1e85343b662abde4ad678887c1b33771a06ed479d98`):
  `I don't have docs, but foochain uses a custom HTTP RPC at
  http://localhost:9999/rpc with JSON requests like
  {"method":"foo_getBlock","params":["latest"]}`
- Next complete agent response (from `session.log`
  sha256=`0ed045505a976927af24cc35d7e8950c7035ef7d9c761166be7bbafb7e37a289`):
  ```
  Agent> [thinking] Processing the current request. Press Ctrl+C to cancel this turn without exiting the Agent.
  Agent> To avoid applying only part of this turn, clarify the following items that were not mapped safely:
  - I don't have docs, but foochain uses a custom HTTP RPC at http://localhost:9999/rpc with JSON requests like {"method":"foo_getBlock","params":["latest"]}
  Provide official protocol/RPC documentation, endpoint documentation, or a request and/or response example. You may submit the part currently available.
  Enter a value.
  ```
- Additional failing turn 5: `no official docs exist, foochain is
  proprietary. Can you handoff to a search or generate a stub adapter?`
  → same generic clarify rejection
- Sonnet abandoned at turn 6 (`exit`) after two unmapped-evidence
  rejections.
- Tool receipt path + SHA-256: none — evidence parsing decision
- Runtime event/checkpoint path + SHA-256: `langgraph-snapshot.tgz`
  sha256=`d7fc3bf5a471c37ad11755714a461d0eb784bf8a2e70614103b935cba212011d`
- Session state path + SHA-256: `session-state.json`
  sha256=`43b0f0535ebc9c1a2841bf04d793ad494d0d7a97fc7f49acc8f30a63fcfcbc20`
- Driver log path + SHA-256: `driver.jsonl`
  sha256=`c51074f922c590594209a1e85343b662abde4ad678887c1b33771a06ed479d98`
- First failing turn index: turn 4
- Expected contract/postcondition: the evidence prompt should either
  (a) extract the JSON fragment `{"method":"foo_getBlock","params":
  ["latest"]}` from the surrounding prose and accept it as a request
  example, or (b) explicitly say "please paste ONLY the JSON request
  example or the raw docs URL; prose descriptions cannot be parsed",
  and offer an "I have no docs" escape hatch that routes to a stub-
  adapter generation flow
- Observed behavior: entire prose is echoed verbatim as unmapped; no
  hint about what format is expected; no escape hatch when the user
  genuinely has no docs; user must type `exit`
- User impact: users with only informal knowledge of a new chain
  (which is REALISTIC for a new-adapter handoff scenario) are blocked;
  the handoff flow only works if the user already has a copy-pastable
  pure-JSON document
- Reproduction count: 2 (turns 4 and 5)
- Fresh isolated session reproduced: yes

## 分类

- Classification: `product_failure`
- Suspected root owner: `semantic_planning` (evidence extractor) or
  `domain:new-adapter-handoff`
- Supporting evidence: this is the fourth instance of the "generic
  clarify" pattern (EXT-20260813-001 language switch,
  EXT-20260813-003 chatty NL, EXT-20260813-004 multi-key env,
  EXT-20260813-005 default method name, EXT-20260813-006 done/skip).
  All point to a single strict-match gate with no NL/mixed-content
  extraction.
- Evidence against stale session/data contamination: fresh session
- Not tested because: (i) pure JSON `{"method":"foo_getBlock",
  "params":["latest"]}` alone not tested; (ii) bare URL alone not
  tested in CASE3 (was tested in CASE2 turn 5, also rejected)

## 安全传输

- Redaction review completed by: Claude Sonnet 4.6 (recorder)
- Bundle path: `.agent/external-validation/a22108e53401cf7d9314917b870b71ccf338619c/transcripts/WW-CASE3-001/`
- Bundle SHA-256: per-file hashes in updated SHA256SUMS
- Internal `SHA256SUMS` verified on personal workstation: `no`
- Missing evidence: pure-JSON-alone variant not tested

## 个人电脑 Codex 确定性修复

- Evidence bundle integrity verified: pending Codex
- Reproduced on exact source revision: pending Codex
- Root cause: pending Codex — hypothesis: the evidence extractor is
  strict about accepting only certain content shapes and has no
  fallback route for "I have no docs; help me generate a stub"
- Violated architecture authority: pending Codex — likely the
  unsupported-adapter handoff contract
- Single authoritative repair owner: `semantic_planning` most likely
- Framework-level repair design: pending Codex — either (a) add a
  fragment extractor that scans the input for JSON, URL, or curl
  patterns and accepts the extracted fragment, or (b) add an explicit
  "I have no docs" option that routes to a stub-adapter generation
  flow with a clear disclaimer
- Files changed: pending Codex
- Repair revision: pending Codex
- Focused deterministic tests: pure JSON input `{"method":"foo",
  "params":[]}`; URL alone; curl command alone; prose+JSON; "I have
  no docs" natural-language phrase
- Adjacent regression tests: valid pure-format evidence still accepted
  (CASE2 evidence bundle collection remains functional)
- Complete Docker/Linux gates: pending Codex
- Why this is not a phrase/keyword patch: the fix must be a real
  fragment extractor or an explicit "no docs" escape hatch, not a
  string-match on the tested phrasing

## 工作电脑回验

- Clean checkout of repair revision: pending
- Original failed journey result: pending
- Adjacent variant 1: pure JSON alone — pending
- Adjacent variant 2: "I have no docs" natural language — pending
- Gemini/ADC/google_search result: pending
- New evidence bundle + SHA-256: pending
- Remaining gap: pending
- Closure decision and reason: pending
