# AnyChain Agent External Validation Record — EXT-20260808-006

Novel-chain classification triggers a >80-second LLM-reasoning round-trip
before responding, instead of a fast lookup against the 36-chain registry
followed by a short confirmation prompt. Latency is 40× longer than
registry-hit paths in adjacent turns.

## 身份

- Record ID: `EXT-20260808-006`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Runtime: Ubuntu 24.04 Docker `blockchain-node-benchmark:test-ubuntu`
- Host: Debian 13 gLinux VM
- Tracked worktree clean: `yes`

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
- External user AI/model: Claude Sonnet 4.6 (this recorder)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`
- Startup evidence sha256s: same as WW-STARTUP-001 startup bundle

## 起始状态

- Manifest scenario ID: `WW-RESUME-001`
- Journey/task ID: post-restart resume path; chain=solana preserved
- Session/checkpoint ID: after modify-menu → chain_identity group,
  agent awaiting a chain-name input
- Chain case: `case2` (unknown chain, existing adapter TBD) or `case3`
  (unsupported adapter with search handoff)
- Target/workflow/RPC/QPS modes: none selected

## 精确观察

- Previous complete agent response path + SHA-256:
  `.agent/external-validation/a22108e5.../transcripts/WW-RESUME-001/session.log`
  sha256=`4a7ae93d1a77d3b99c4f5cef8938a4ca5d13a943b2594c7834fc0be35ee1
  4303` — the chain-switch input prompt "请输入要切换到的链名。当前链
  solana 会保留到新链被确认。请直接输入值。"
- Exact Claude user input path + SHA-256: same session.log,
  input string `not-a-real-chain` (turn 3)
- Timing observation: input arrived at driver-measured T=0s; first
  agent response chunk (`[thinking]` line) appeared within ~1s;
  full response (novel-chain confirmation dialog) appeared at T≈80s
- Next complete agent response path + SHA-256: same session.log,
  the novel-chain confirmation dialog quoted in EXT-20260808-005
- Tool receipt path + SHA-256: none observed in session.log (no
  tool-invocation trace surfaced in output); langgraph-snapshot.tgz
  sha256=`d597f955bf95493054cc543775a60b8fd54defd8f764fc8f17326e23a8fc
  3ebd` contains the checkpointer state and may include event-level
  timings for Codex analysis
- Job/log/CSV/HTML path + SHA-256: N/A
- First failing turn index: turn 3
- Expected contract/postcondition: novel-chain input should be
  classified in ≤2 seconds via a registry lookup against the loaded
  36-chain list (startup line "已加载框架事实：36 chains, 6 adapter
  families, 109 RPC methods"). Chain names not in the registry should
  route directly to the case2/case3 confirmation dialog without an
  intervening LLM reasoning pass, or route to an explicit web-research
  intent that clearly informs the user "正在联网确认此链" while the
  research runs. Silent 80-second delay with only the generic
  "[thinking] 正在处理当前请求" indicator is unacceptable UX.
- Observed behavior: agent emits "[thinking]" chunk within ~1s of
  input receipt, then produces no output for ~79 more seconds, then
  emits the novel-chain confirmation dialog. During the 79-second
  silent interval, user cannot distinguish (a) LLM is reasoning, (b)
  web-research call is running, (c) agent has hung, (d) network
  round-trip stalled. Session state does not reflect any pending
  research operation.
- User impact: severe UX degradation. Any user typing a typo or a
  chain name they misremember will experience a ~80-second silent
  wait with no indication of what is happening. In multi-tenant or
  automated pipeline usage this could trigger timeout retries or
  abandonment. Comparison with turn 4 (2s to cancel), turn 5 (also
  fast, went to orientation menu), and turn 6 (fast) shows this
  latency is specific to novel-chain classification, not general
  agent latency.
- Reproduction count: 1
- Fresh isolated session reproduced: `no` (pending Codex)

## 分类

- Classification: `product_failure` (performance / UX)
- Suspected root owner: `semantic_planning` (primary hypothesis: chain
  classification path calls an LLM reasoning step to decide "is this a
  real chain the user knows about but we don't yet support?" instead
  of consulting the loaded chain registry first) OR `domain:chain-
  registry` (secondary hypothesis: registry lookup is bypassed for
  unknown-prefix inputs)
- Supporting evidence:
  - Startup line reports "已加载框架事实：36 chains, 6 adapter
    families, 109 RPC methods" — chain registry is loaded in-process
  - Turn 3 free-form chain-list query in WW-STARTUP-001 (referenced
    session.log L77-L79) returned the full 36-chain enumeration in
    ~5 seconds — registry lookup path is fast when the answer is
    "here is the list"
  - Turn 4 cancel dispatch (WW-RESUME-001) returned in ~1 second —
    dispatch path is not the bottleneck
  - Only novel-chain classification incurs the 80-second latency —
    localizing to that specific decision path
- Evidence against stale session/data contamination: agent process
  pid 4533 stable throughout; no restart or checkpoint reload
  between turn 2 (which returned in ~1s) and turn 3 (which took
  80s); same chain-registry state loaded for both turns
- Not tested because: (i) LLM raw request/response payload not
  captured — Codex will need instrumented replay or provider-layer
  logs to confirm this is an LLM reasoning call vs. a stuck web-
  research call; (ii) network trace to Vertex not captured; (iii)
  same input with different chain names (e.g., "solanaa", "eth")
  not repeated to characterize the latency distribution

## 安全传输

- Redaction review completed by: Claude Sonnet 4.6 (this recorder)
- Bundle path: `.agent/external-validation/a22108e5.../transcripts/
  WW-RESUME-001/`
- Bundle SHA-256: per-file hashes in updated SHA256SUMS
- Internal `SHA256SUMS` verified on personal workstation: `no`
- Missing evidence: per-request LLM payload logs; network trace

## 个人电脑 Codex 确定性修复

- Reproduced on exact source revision: `no` (pending)
- Root cause: (Codex to determine — hypothesis A: chain-name intent
  handler unconditionally calls LLM for novelty reasoning even when
  a registry-membership check is O(1); hypothesis B: web-research
  call is initiated silently and stalls waiting for response even
  though the provider reports web_research unavailable at startup)
- Violated architecture authority: (Codex — performance contract
  on classification steps; ADR reference TBD)
- Single authoritative repair owner: (Codex — likely
  `semantic_planning` or a chain-registry-lookup helper)
- Framework-level repair design: (Codex — must add registry-first
  fast path; may add explicit user-visible progress indicator for
  any classification call that exceeds N seconds)
- Files changed: (Codex)
- Repair revision: (Codex)
- Focused deterministic tests: (Codex — classification of known
  chain names in <2s; classification of clearly-invalid strings in
  <2s; visible progress for any operation >5s)
- Adjacent regression tests: (Codex — registry cache invalidation,
  concurrent classification requests, timeouts)
- Why this is not a phrase/keyword/transcript patch: the fix is
  algorithmic (add registry lookup as first step of classification)
  and observable (latency numeric target), not a surface string

## 工作电脑回验

- Original failed journey result: pending
- Adjacent variant 1: chain-name typo ("solanaa") — pending
- Adjacent variant 2: capitalization variant ("SOLANA") — pending
- Remaining gap: pending
- Closure decision and reason: pending
