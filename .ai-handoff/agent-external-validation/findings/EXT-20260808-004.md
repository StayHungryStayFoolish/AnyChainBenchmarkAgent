# AnyChain Agent External Validation Record — EXT-20260808-004

Orientation menu option "4" numeric selection produces shallow self-intro
fallback, NOT the promised drill-down of "supported chains, RPC methods, and
extension paths"; option label thus violates its own contract.

## 身份

- Record ID: `EXT-20260808-004`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Worktree hash: computed at turn boundary — see runtime event bundle
- Tracked worktree clean: `yes`
- Runtime: Ubuntu 24.04 in Docker image `blockchain-node-benchmark:test-ubuntu`
  (id `a0f7d80d1b47`, container `bench-preflight`)
- Host: Debian 13 gLinux VM, x86_64

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
  (Leland-authorized substitution — discovery-only)
- External user AI/model: Claude Sonnet 4.6 (this recorder)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`
- `llm-config` evidence: `.agent/external-validation/a22108e5.../provider-status.json`
  sha256=`8616d45872f17429cb4c3db4804efe2646b581e0dd947136cd4159b732125e9f`
- `doctor` evidence: `.agent/external-validation/a22108e5.../doctor.json`
  sha256=`780b0628b50c946793169131a41af5ab30b61619ea5fe126fd0593b69dede366`
- `llm-smoke` evidence: `.agent/external-validation/a22108e5.../llm-smoke.json`
  sha256=`c6adcc50a9f1178d36f6085773215366a9ee8542ffdc32b54ba87780f2fd8592`
- `adk-status` evidence: `.agent/external-validation/a22108e5.../adk-status.json`
  sha256=`6e4b77c8eabc5a07c562367056cfca3320c6a28852109d9561fb4255ae5bf2a6`
- 实际 google_search typed receipt/citations: N/A

## 起始状态

- Manifest scenario ID: `WW-STARTUP-001`
- Journey/task ID: same session as EXT-20260808-002/003
- Session/checkpoint ID: after turn 1, orientation menu re-emitted in Chinese
  (session.log L54-L58), option 4 label: "4. 了解支持的链、RPC method 和二次
  开发方式 — 只读查看框架能力、默认 workload 与扩展路径，不修改测试配置。"
- Starting groups and states: session locale zh, no chain bound, no pending
  question
- Pending question ID/owner: none — orientation menu is user-initiated choice
  point
- Chain case: N/A (option 4 is chain-agnostic capability drill-down)
- Target/workflow/RPC/QPS modes: none
- Prior job ID/status: none

## 精确观察

- Previous complete agent response path + SHA-256:
  `.agent/external-validation/a22108e5.../transcripts/WW-STARTUP-001/session.log`
  sha256=`2c678e8058617dba798525e2b9e02efa2f62c6293855b3a405d630f791c56bc2`
  (turn-1 response L50-L58 emits the four-option orientation menu with option
  4 label "了解支持的链、RPC method 和二次开发方式 — 只读查看框架能力、默认
  workload 与扩展路径")
- Exact Claude user input path + SHA-256: same session.log L66,
  input string `4` (turn 2, numeric option selection)
- Next complete agent response path + SHA-256: same session.log L67-L69,
  output string
  ```
  Agent> [thinking] 正在处理当前请求。按 Ctrl+C 可取消本轮，不会退出 Agent。
  Agent> 我是 AnyChain Benchmark Agent。我支持 fake-node 闭环验证、real-node
         RPC 压测、sync-observe 同步观察、自定义 RPC、新链协议适配、preflight/
         smoke、job 跟踪，以及日志和报告分析。
  当前框架事实：36 chains, 6 adapter families, 109 RPC methods.
  ```
  This is a byte-identical repeat of the self-intro from turn-1 pre-menu
  paragraph (session.log L51-L52), with NO chain list, NO RPC method
  enumeration, NO default workload information, and NO extension-path
  documentation. Missing: everything the menu label promised.
- Tool receipt path + SHA-256: none (no drill-down tool call was invoked)
- Runtime event/checkpoint path + SHA-256:
  `.agent/external-validation/a22108e5.../transcripts/WW-STARTUP-001/langgraph-snapshot.tgz`
  sha256=`2fb3dde546eea778584568a622d8833345f0d15ea8987ba5c29f5cdf696d17f5`
- Job/log/CSV/HTML path + SHA-256: N/A
- First failing turn index: turn 2
- Expected contract/postcondition: option "4" numeric selection should
  dispatch to a capability drill-down flow that at minimum emits (a) the
  configured chain list (as turn 3 demonstrates the capability exists —
  L79 "acala, algorand, aptos, ..." full 36-chain enumeration), (b) a
  summary of RPC method families (109 methods across 6 adapter families
  per startup line L6), (c) default workload templates or a pointer to
  per-chain workload rendering (as turn 6 demonstrates capability exists),
  and (d) extension path documentation ("二次开发方式" per label). The
  label is a promise; the response must match.
- Observed behavior: numeric "4" selection is routed to the generic
  self-intro handler, which emits the same paragraph as the initial
  orientation preamble. Menu breadcrumbs terminate at a dead end — user
  is returned to the User> prompt with no drill-down content and no
  indication that the option is not implemented or that the numeric
  dispatch failed.
- User impact: any user following menu-based navigation (a documented
  affordance per L34/L58 "Reply with an option number or name") will
  discover option 4 is decorative. The three real capabilities (start
  benchmarks — options 1-3) may work; option 4's drill-down does not.
  This breaks user trust in menu navigation and forces users to fall
  back to natural-language phrasing (which per turn 3 does work for the
  chain-list slice, but option 4's fuller drill-down remains unreachable
  by any tested path).
- Reproduction count: 1 (turn 2)
- Fresh isolated session reproduced: `no` (pending Codex replay)

## 分类

- Classification: `product_failure`
- Suspected root owner: `terminal` (primary hypothesis: numeric menu
  dispatch in `agent/terminal/repl.py` has a switch that handles 1/2/3 for
  benchmark start flows but option 4 falls through to a default self-intro
  fallback) OR `semantic_planning` (secondary hypothesis: option-4-specific
  drill-down intent is not registered)
- Supporting evidence:
  - Turn 3 free-text query "请列出支持的所有链名称" (L77) → full 36-chain
    enumeration (L79). Chain-list capability exists and is invokable via
    natural language.
  - Turn 6 free-text query for workload after chain binding → real
    workload template (L129). Workload rendering capability exists.
  - Turn 2 numeric "4" → generic self-intro only. The gap is specifically
    the numeric-option-to-drill-down dispatch.
  - Startup line L34 "Try: benchmark solana, use fake-node, doctor, jobs,
    status, logs <job_id>, follow <job_id>, help, exit." lists eight
    meta-commands but does not include a drill-down meta-command that could
    substitute for menu option 4. There is no discoverable alternative to
    "select 4" for the promised drill-down flow.
- Evidence against stale session/data contamination: fresh session; menu
  emitted in-session; numeric input is a single character with no
  ambiguity
- Not tested because: (i) English numeric "4" input on English orientation
  menu not tested; (ii) "option 4" or "选项 4" name-form input not tested
  (menu says "Reply with an option number or name"); (iii) options 1-3
  numeric dispatch not tested — cannot confirm this is option-4-specific
  or a broader numeric-dispatch failure; adjacent-scenario coverage of
  benchmark start flows (WW-EXEC-001) will test options 1-3 dispatch.

## 安全传输

- Redaction review completed by: Claude Sonnet 4.6 (this recorder)
- Bundle path: `.agent/external-validation/a22108e5.../transcripts/WW-STARTUP-001/`
- Bundle SHA-256: not yet packaged; per-file hashes in per-scenario
  SHA256SUMS updated in same commit
- Internal `SHA256SUMS` verified on personal workstation: `no` (pending)
- Missing evidence: no dispatch-path trace; would benefit from a runtime
  log showing which module received the numeric "4" input and what
  intent classification/handler was selected

## 个人电脑 Codex 确定性修复

- Evidence bundle integrity verified: `no` (pending)
- Reproduced on exact source revision: `no` (pending)
- Root cause: (Codex to determine — hypothesis: numeric menu dispatch table
  covers option 1-3 (benchmark start flows) but option 4 has no registered
  handler and falls through to default; OR option 4 intent is registered
  but its handler is a stub that emits self-intro)
- Violated architecture authority: (Codex to determine)
- Single authoritative repair owner: (Codex to determine — likely
  `terminal` or `semantic_planning`)
- Framework-level repair design: (Codex to determine — must not be a
  keyword patch on the option-4 label; the fix must implement or wire
  the actual drill-down flow)
- Files changed: (Codex to fill)
- Repair revision: (Codex to fill)
- Focused deterministic tests: (Codex to fill — must cover: numeric "4"
  input in Chinese session emits drill-down; name-form "了解支持的链..."
  input emits equivalent drill-down; English session numeric "4" emits
  English drill-down)
- Adjacent regression tests: (Codex to fill — options 1-3 numeric
  dispatch unchanged; free-form chain queries still work)
- Complete Docker/Linux gates: (Codex to fill)
- Why this is not a phrase/keyword/transcript/terminal/verifier patch:
  (Codex to argue — proposed fix wires an actual drill-down handler; not
  a re-label of option 4 to match the current shallow behavior)

## 工作电脑回验

- Clean checkout of repair revision: `no` (pending)
- Original failed journey result: pending (turn 2 numeric "4" must emit
  drill-down content post-repair)
- Adjacent variant 1: name-form input (e.g., "了解支持的链、RPC method
  和二次开发方式") — should reach same drill-down
- Adjacent variant 2: English session numeric "4" — should emit English
  drill-down
- Gemini/ADC/google_search result: N/A
- New evidence bundle + SHA-256: pending
- Remaining gap: pending
- Closure decision and reason: pending
