# AnyChain Agent External Validation Record — EXT-20260808-005

Novel-chain confirmation dialog offers a malformed cancel option labeled
`2. N` (single letter) instead of an explicit `2. Cancel` / `2. 取消`
label; option label violates orientation convention used elsewhere.

## 身份

- Record ID: `EXT-20260808-005`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Runtime: Ubuntu 24.04 Docker `blockchain-node-benchmark:test-ubuntu`
  container `bench-preflight`
- Host: Debian 13 gLinux VM, x86_64
- Tracked worktree clean: `yes`

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
  (Leland-authorized discovery substitution)
- External user AI/model: Claude Sonnet 4.6 (this recorder)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`
- Startup evidence sha256s: see SHA256SUMS (unchanged from
  WW-STARTUP-001 startup bundle)

## 起始状态

- Manifest scenario ID: `WW-RESUME-001`
- Journey/task ID: post-WW-STARTUP-001 restart; agent detected prior
  session with `chain: solana` (confirmed field BLOCKCHAIN_NODE)
- Session/checkpoint ID: langgraph checkpointer state persisted across
  REPL restart (see langgraph-snapshot.tgz below)
- Starting groups and states: after turn 1 ("2" — 保留已确认值并选择要
  修改的配置) → modify menu; turn 2 ("2" — chain_identity group) →
  chain-switch prompt "请输入要切换到的链名。当前链 solana 会保留到新链
  被确认。"
- Pending question ID/owner: chain-switch input awaiting user value
- Chain case: `case2` or `case3` boundary — user attempts unknown chain
  name, agent must classify as either new adapter needed or search
  handoff
- Target/workflow/RPC/QPS modes: none — all still unselected
- Prior job ID/status: none

## 精确观察

- Previous complete agent response path + SHA-256: `.agent/external-
  validation/a22108e5.../transcripts/WW-RESUME-001/session.log`
  sha256=`4a7ae93d1a77d3b99c4f5cef8938a4ca5d13a943b2594c7834fc0be35ee1
  4303` (byte range for the chain-input prompt on session.log line "请
  输入要切换到的链名。当前链 solana 会保留到新链被确认。")
- Exact Claude user input path + SHA-256: same session.log,
  input string `not-a-real-chain` (turn 3)
- Next complete agent response path + SHA-256: same session.log,
  output text:
  ```
  Agent> `not-a-real-chain` 尚未配置，也未被可靠确认属于已支持协议。
         是否从 `solana` 切换到这条真实链并确认协议，还是取消？
  1. 真实链，继续确认协议
  2. N
  请回复选项编号或名称。
  ```
- Tool receipt path + SHA-256: none (this appears to be a label
  formatting issue upstream of any tool call)
- Runtime event/checkpoint path + SHA-256: langgraph-snapshot.tgz
  sha256=`d597f955bf95493054cc543775a60b8fd54defd8f764fc8f17326e23a8fc
  3ebd`
- Session state path + SHA-256: session-state.json sha256=
  `efebb3ec3265a2bb45a3974f45d8ac33475b598895fac8f67ae236466d7f35dd`
  (language zh, no pending question, unchanged from WW-STARTUP-001)
- First failing turn index: turn 3 (agent's response to "not-a-real-
  chain")
- Expected contract/postcondition: cancel option in a 2-choice
  confirmation dialog must have a self-describing label matching the
  action, consistent with orientation menu convention which uses
  descriptive phrases (e.g., "1. 真实链，继续确认协议"). Options like
  "2. Cancel", "2. 取消", or "2. 保持当前链 (solana)" would be
  contract-conformant. `2. N` is a single character with no clear
  action semantics.
- Observed behavior: cancel option label rendered as literal single
  character `N`. When user then sends "2" (turn 4), agent DOES treat
  it as cancel and returns to orientation menu — proving the dispatch
  behind the label is correct; only the label rendering is wrong.
- User impact: user confusion — the label "N" reads either as (a) a
  yes/no answer letter that leaked into option label position, (b) an
  incomplete render truncation, or (c) some placeholder. A user
  reading the menu cannot infer that pressing 2 will cancel and
  preserve the current chain. Discoverable only by trial-and-error.
- Reproduction count: 1
- Fresh isolated session reproduced: `no` (needs Codex replay)

## 分类

- Classification: `product_failure`
- Suspected root owner: `response_authority` (label rendering) OR
  `semantic_planning` (option-label choice at dialog-construction
  time)
- Supporting evidence: option 1 has a descriptive label ("真实链，继续
  确认协议"); orientation menu labels throughout the transcript are
  descriptive phrases with em-dash separator; the "N" label is
  inconsistent with the entire label-format convention observed
  elsewhere in this session
- Evidence against stale session/data contamination: response was
  generated in real-time in response to turn 3 input; agent process
  running (pid 4533)
- Not tested because: (i) whether the same dialog in English shows "2.
  N" or "2. No" not tested; (ii) whether other confirm/cancel dialogs
  in the codebase have the same label bug not tested

## 安全传输

- Redaction review completed by: Claude Sonnet 4.6 (this recorder)
- Bundle path: `.agent/external-validation/a22108e5.../transcripts/
  WW-RESUME-001/`
- Bundle SHA-256: per-file hashes in updated SHA256SUMS
- Internal `SHA256SUMS` verified on personal workstation: `no`

## 个人电脑 Codex 确定性修复

- Reproduced on exact source revision: `no` (pending Codex)
- Root cause: (Codex to determine — hypothesis: dialog-construction
  code path picks option label from a mapping that has "N" for
  cancel; likely a truncated or placeholder value slipped through)
- Violated architecture authority: (Codex to determine — likely a
  UX contract on menu label discoverability)
- Single authoritative repair owner: `response_authority` (most
  likely)
- Framework-level repair design: fix must actually render a
  descriptive label; NOT a keyword patch on "N"
- Files changed: (Codex)
- Repair revision: (Codex)
- Focused deterministic tests: (Codex — test dialog construction
  produces descriptive labels for all confirm/cancel dialogs)
- Adjacent regression tests: (Codex — Chinese and English locale
  label rendering)
- Why this is not a phrase/keyword/transcript patch: the current
  behavior is CORRECT (cancel dispatches correctly); the fix is
  purely in label-generation code, not surface strings

## 工作电脑回验

- Original failed journey result: pending
- Adjacent variant 1: English-locale novel-chain dialog — pending
- Adjacent variant 2: novel chain with different name — pending
- Remaining gap: pending
