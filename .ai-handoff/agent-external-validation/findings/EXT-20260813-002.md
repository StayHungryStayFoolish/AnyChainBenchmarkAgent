# AnyChain Agent External Validation Record — EXT-20260813-002

Chinese natural-language affirmative/negative answers (`是的`, `不用，换个`)
are rejected as unmapped "clarify" content on Y/N confirmation prompts,
even though the surrounding question is emitted in Chinese and English
equivalents (`yes`, `Y`, `y`, `n`) are accepted. Terse Chinese words map
to the generic clarify bucket while identical-semantic terse English
maps to the correct choice.

## 身份

- Record ID: `EXT-20260813-002`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Worktree hash: N/A (detached in-container checkout)
- Tracked worktree clean: `yes`
- Runtime: Ubuntu Docker `blockchain-node-benchmark:test-ubuntu`
  container `bench-preflight`
- Host: Debian 13 gLinux GCE VM, x86_64

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
  (Leland-authorized discovery substitution)
- External user AI/model: Claude Sonnet 4.6 (Vertex `us-east5` rawPredict,
  discovery substitution for manifest role `gemini_via_vertex_adc`)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`
- Preflight evidence hashes (shared bundle): see `PASS-WW-STARTUP-001-rerun`
  or `SHA256SUMS` at bundle root
- 实际 google_search typed receipt: N/A

## 起始状态

- Manifest scenario ID: `WW-LANGUAGE-001`
- Journey/task ID: fresh cold-start, no prior job
- Session/checkpoint ID: fresh langgraph state at REPL launch
- Starting groups and states: cold-start banner then orientation menu
- Pending question ID/owner: none at scenario start; a
  `CLOUD_REGION` Y/N confirmation is pending after turns 1-2
- Chain case: `supported` (`ethereum`)
- Target/workflow/RPC/QPS modes: turn 1 selects fake-node then turn 2
  selects ethereum; other fields chosen mid-scenario
- Prior job ID/status: none

## 精确观察

- Previous complete agent response for turn 3: `session.log`
  sha256=`74c120a0f8e2ebbf7a957d916afea64170b5c84423bc8d7d3eb5bad07b174a9e`
  — the pending prompt (after turn 2 accepted `eth` → `ethereum`) reads:
  ```
  Agent> Confirmed chain: `ethereum`.
  Detected CLOUD_REGION: `us-central1`. Use this value?
  1. Y
  2. N
  Reply with an option number or name, or enter a custom value.
  ```
- Exact Sonnet user input for turn 3 (verbatim from `driver.jsonl`
  sha256=`5105e9f0455a8d8935a50e9ba35f010029b370da1a06d2dfbce425365c75cd7f`):
  `是的`
- Next complete agent response for turn 3 (from session.log):
  ```
  Agent> [thinking] 正在处理当前请求。按 Ctrl+C 可取消本轮，不会退出 Agent。
  Agent> 为了避免只应用你这一轮的部分需求，请先澄清以下尚未安全映射的内容：
  - 是的
  检测到 CLOUD_REGION 为 `us-central1`，是否使用？
  1. Y
  2. N
  请回复选项编号或名称，也可以直接输入自定义值。
  ```
  — The pending question is re-emitted in Chinese (since input was Chinese),
  but the Chinese affirmative `是的` was NOT mapped to option 1 (Y).
- Additional failing input: turn 6 `不用，换个` (Chinese for "no, change
  it") on the MACHINE_TYPE Y/N confirmation → same clarify template,
  re-emits question in Chinese.
- Contrast — successful English affirmatives on identical Y/N prompt:
  - turn 4 `Y` → agent proceeds to next question (`MACHINE_TYPE` prompt)
  - turn 5 `yes` → agent proceeds to next question
  - turn 11 `y` → agent proceeds
  - turn 20 `Y` → agent proceeds
  - turn 14 `n` → agent treats as negative and proceeds
- Contrast — successful Chinese short answers WHEN they are exact option
  names or numbers:
  - turn 7 `2` → interpreted as "N" for MACHINE_TYPE confirmation
  - turn 12 `single` (English) → interpreted correctly
- Tool receipt path + SHA-256: none — this is intent routing
- Runtime event/checkpoint path + SHA-256: `langgraph-snapshot.tgz`
  sha256=`5f52e0777497158642083602649e8de6319056ccd246d87fbab304ed276cf167`
- Session state path + SHA-256: `session-state.json`
  sha256=`efebb3ec3265a2bb45a3974f45d8ac33475b598895fac8f67ae236466d7f35dd`
  content: `{"current_question_id":"","language":"zh","pending_missing_dependencies":[]}`
  — `language: zh` at scenario end (implicit tracking after enough
  Chinese input; contrast with WW-STARTUP-001 where it stayed `en`
  despite Chinese input — see EXT-20260813-001)
- Driver log path + SHA-256: `driver.jsonl`
  sha256=`5105e9f0455a8d8935a50e9ba35f010029b370da1a06d2dfbce425365c75cd7f`
- First failing turn index: turn 3 (`是的`)
- Expected contract/postcondition: bilingual CLI must accept the most
  common Chinese affirmatives / negatives (`是`, `是的`, `好的`, `确认`,
  `不`, `不用`, `否`, `取消`) as equivalents of option `Y`/`N` /
  numeric `1`/`2` when the pending question is a two-option confirm.
  If tool intentionally requires strict option names, the initial prompt
  must state that explicitly (in Chinese too) so users know terse
  Chinese won't work.
- Observed behavior: the entire class of Chinese terse
  affirmative/negative words is unmapped. The generic clarify template
  is used for `是的` and `不用，换个`. The pending prompt IS displayed
  in Chinese, so users reasonably expect Chinese answers to work.
- User impact: users typing Chinese answers are silently corrected via
  the generic clarify template that says "the following was not mapped
  safely", listing their input verbatim. This creates a frustrating
  loop where the user must translate their answer to English or pick a
  number. Combined with EXT-20260813-001 (no language-switch command),
  a Chinese-preference user cannot get a smooth flow.
- Reproduction count: 2 (`是的` on CLOUD_REGION Y/N + `不用，换个` on
  MACHINE_TYPE Y/N) plus implicit reproduction across every Y/N
  confirmation in the config flow
- Fresh isolated session reproduced: yes (this scenario itself is a
  fresh session)

## 分类

- Classification: `product_failure`
- Suspected root owner: `semantic_planning` (Y/N intent mapping)
  or `domain:config-flow` (whichever module owns the Y/N confirmation
  mapping)
- Supporting evidence: identical prompts accept English affirmatives
  perfectly on turns 4, 5, 11, 20 in the same session. The failure is
  strictly for Chinese phrasings.
- Evidence against stale session/data contamination: fresh `.agent/`
  state at scenario start; the Chinese input works elsewhere (e.g.,
  turn 7 `2` after re-prompt) confirming the state is intact
- Not tested because: (i) Cantonese variants (`係`, `唔要`) not tested;
  (ii) formal Chinese (`是`, `否`) short forms not tested; (iii)
  affirmative with punctuation `是的.` or `是的！` not tested

## 安全传输

- Redaction review completed by: Claude Sonnet 4.6 (recorder)
- Bundle path: `.agent/external-validation/a22108e53401cf7d9314917b870b71ccf338619c/transcripts/WW-LANGUAGE-001/`
- Bundle SHA-256: per-file hashes in updated SHA256SUMS
- Internal `SHA256SUMS` verified on personal workstation: `no`
- Missing evidence: none

## 个人电脑 Codex 确定性修复

- Evidence bundle integrity verified: pending Codex
- Reproduced on exact source revision: pending Codex
- Root cause: pending Codex (hypothesis: the Y/N mapper only accepts
  `Y`, `y`, `yes`, `N`, `n`, `no`, `1`, `2`, and numeric option index
  variants; no Chinese equivalents)
- Violated architecture authority: pending Codex (likely a bilingual UX
  contract in the response authority or a routing table gap)
- Single authoritative repair owner: `semantic_planning` most likely
- Framework-level repair design: extend the Y/N intent mapper to accept
  Chinese affirmatives / negatives (`是` / `是的` / `好` / `好的` /
  `确认` / `确定` / `Y` / `y` / `yes` for Y; `否` / `不` / `不用` /
  `取消` / `N` / `n` / `no` for N). NOT a substring match on the two
  phrasings I tested; a real bilingual synonym set in the mapper.
- Files changed: pending Codex
- Repair revision: pending Codex
- Focused deterministic tests: `是的` and `不用，换个` on both
  CLOUD_REGION and MACHINE_TYPE Y/N prompts, plus mixed-locale prompt
  where question is English but answer is Chinese and vice-versa
- Adjacent regression tests: English `yes`/`Y`/`y`/`n`/`no` still map
  correctly; numeric `1`/`2` still map; free-text (like `n2-standard-8`)
  still passes through to the answer bucket
- Complete Docker/Linux gates: pending Codex
- Why this is not a phrase/keyword patch: the fix must be an explicit
  bilingual synonym set in the confirmation-intent mapper, not a
  post-hoc string match; must also cover future Chinese variants via
  synonym normalization

## 工作电脑回验

- Clean checkout of repair revision: pending
- Original failed journey result: pending
- Adjacent variant 1: `是` (bare) on Y/N prompt — pending
- Adjacent variant 2: `否` on Y/N prompt — pending
- Gemini/ADC/google_search result: pending
- New evidence bundle + SHA-256: pending
- Remaining gap: pending
- Closure decision and reason: pending
