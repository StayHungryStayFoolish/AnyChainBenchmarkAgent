# AnyChain Agent External Validation Record — EXT-20260808-003

Session locale field `language: "zh"` is persisted in session-state.json but
NOT consulted at response-generation time; a single English user input causes
silent language flip from Chinese to English mid-session while session state
still shows `zh`.

## 身份

- Record ID: `EXT-20260808-003`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Worktree hash: computed at turn boundary — see runtime event bundle
- Tracked worktree clean: `yes`
- Runtime: Ubuntu 24.04 in Docker image `blockchain-node-benchmark:test-ubuntu`
  (id `a0f7d80d1b47`, container `bench-preflight`)
- Host: Debian 13 gLinux VM, x86_64

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
  (Leland-authorized substitution — discovery-only)
- External user AI/model: Claude Sonnet 4.6 (this recorder)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`
- `llm-config` evidence: `.agent/external-validation/a22108e5.../provider-status.json`
  sha256=`8616d45872f17429cb4c3db4804efe2646b581e0dd947136cd4159b732125e9f`
- `doctor` evidence: `.agent/external-validation/a22108e5.../doctor.json`
  sha256=`780b0628b50c946793169131a41af5ab30b61619ea5fe126fd0593b69dede366`
- `llm-smoke` evidence: `.agent/external-validation/a22108e5.../llm-smoke.json`
  sha256=`c6adcc50a9f1178d36f6085773215366a9ee8542ffdc32b54ba87780f2fd8592`
- `adk-status` evidence: `.agent/external-validation/a22108e5.../adk-status.json`
  sha256=`6e4b77c8eabc5a07c562367056cfca3320c6a28852109d9561fb4255ae5bf2a6`
- 实际 google_search typed receipt/citations: N/A

## 起始状态

- Manifest scenario ID: `WW-STARTUP-001`
- Journey/task ID: same fresh session as EXT-20260808-002
- Session/checkpoint ID: shared; language field established as `zh` at turn 1
  (session.log L42 first Chinese input "你好，请用中文介绍一下你能做什么" →
  L50-L58 Chinese response with translated menu)
- Starting groups and states: `session_state.language = "zh"` after turn 1,
  persisted through turn 4 (all in Chinese)
- Pending question ID/owner: none
- Chain case: irrelevant to this finding
- Target/workflow/RPC/QPS modes: none
- Prior job ID/status: none

## 精确观察

- Previous complete agent response path + SHA-256:
  `.agent/external-validation/a22108e5.../transcripts/WW-STARTUP-001/session.log`
  sha256=`2c678e8058617dba798525e2b9e02efa2f62c6293855b3a405d630f791c56bc2`
  (turn-4 response L95-L96 was in Chinese)
- Exact Claude user input path + SHA-256: same session.log L104,
  input string `use solana` (turn 5, English)
- Next complete agent response path + SHA-256: same session.log L105-L112,
  output string
  ```
  Agent> [thinking] Processing the current request. Press Ctrl+C to cancel
         this turn without exiting the Agent.
  Agent> Confirmed chain: `solana`.
  I am AnyChain Benchmark Agent. What would you like help with? State a test
  goal or choose the next step.
  1. Start a fake-node benchmark — Fast, low-risk framework validation with
     recorded fixtures; ...
  2. Start a real-node benchmark — Load-test a reachable real-node RPC
     endpoint and analyze performance bottlenecks.
  3. Start sync-observe (real-node synchronization) — ...
  4. Learn supported chains, RPC methods, and extension paths — ...
  Reply with an option number or name.
  ```
  Entire response is English despite session-state.language="zh".
- Tool receipt path + SHA-256: none
- Runtime event/checkpoint path + SHA-256:
  `.agent/external-validation/a22108e5.../transcripts/WW-STARTUP-001/langgraph-snapshot.tgz`
  sha256=`2fb3dde546eea778584568a622d8833345f0d15ea8987ba5c29f5cdf696d17f5`
- Session state path + SHA-256:
  `.agent/external-validation/a22108e5.../transcripts/WW-STARTUP-001/session-state.json`
  sha256=`efebb3ec3265a2bb45a3974f45d8ac33475b598895fac8f67ae236466d7f35dd`
  contents: `{"current_question_id": "", "language": "zh",
  "pending_missing_dependencies": []}`
- Job/log/CSV/HTML path + SHA-256: N/A
- First failing turn index: turn 5
- Expected contract/postcondition: session locale is persisted in
  session-state.json; the response formatter must consult session locale as
  the primary source and only fall through to input-locale detection when no
  session locale has been established. Once a session locale is set (turn 1
  established `zh`), subsequent responses must remain in that locale unless
  the user explicitly requests a change (e.g., "switch to English", "从现在
  开始用英文回答"). A single English utterance to change chain binding
  (`use solana`) must not implicitly change response locale.
- Observed behavior: response formatter appears to select locale from the
  detected language of the current user input, ignoring the persisted session
  locale entirely. Evidence: (i) session.log L129 (turn 6, Chinese input
  after English turn 5) → Chinese response — this confirms per-turn
  detection, not session persistence; (ii) session-state.json post-session
  still shows `language: "zh"` — proving persistence writes happen, but the
  persisted value is not consulted at response-generation time.
- User impact: mixed-language sessions produce jarring silent language flips.
  A user typing a short English meta-command (like `use solana`, `help`,
  `exit`) in an otherwise Chinese session receives an English response with
  no indication of why the language changed. This degrades transcript
  readability for evaluators, breaks locale consistency contracts, and gives
  the impression that session state is a passive log rather than an active
  controller.
- Reproduction count: 1 (turn 5). Turn 6 provides the contrast (Chinese input
  → Chinese response), and session-state.json provides the smoking-gun
  evidence that session locale field is written but not read.
- Fresh isolated session reproduced: `no` (pending Codex replay)

## 分类

- Classification: `product_failure`
- Suspected root owner: `response_authority` (primary hypothesis: response
  formatter reads input locale rather than session locale) OR `terminal`
  (secondary hypothesis: locale dispatch happens in REPL layer, downstream of
  session state read)
- Supporting evidence:
  - session-state.json final value = `{"language": "zh", ...}` — persistence
    layer writes the field
  - Turn 5 response is entirely English — response formatter did not consult
    the persisted field
  - Turn 6 (Chinese input) restores Chinese response — confirms per-turn
    detection is the active selector
  - Turn 7 exit response ("已退出 AnyChain Benchmark Agent。", L144, Chinese)
    matches turn 7 input "exit" (English) — actually, this line is Chinese
    despite English input, suggesting exit-command response may hardcode
    session locale, unlike normal responses. If confirmed, this narrows the
    bug to non-exit response paths only.
- Evidence against stale session/data contamination: fresh container session,
  no prior `.agent/` state, session-state.json created within this session
  at turn 1. Language field transitions can be traced turn-by-turn in the
  transcript.
- Not tested because: (i) explicit locale-switch commands ("switch to
  English", "从现在开始用中文") not tested — would isolate whether user has
  any mechanism to lock locale; (ii) locale behavior across restart (`.agent/`
  survives, restart, first input in different locale) not tested — would
  determine whether persistence-restore path has the same bug or a different
  one; (iii) intra-response mixed language (Chinese label + English body)
  not tested.

## 安全传输

- Redaction review completed by: Claude Sonnet 4.6 (this recorder)
- Bundle path: `.agent/external-validation/a22108e5.../transcripts/WW-STARTUP-001/`
- Bundle SHA-256: not yet packaged; per-file hashes in per-scenario
  SHA256SUMS updated in same commit
- Internal `SHA256SUMS` verified on personal workstation: `no` (pending)
- Missing evidence: no per-turn session-state.json snapshots (only
  final-state file captured — would strengthen the "persistence writes
  succeed at each turn" claim); no runtime event log showing which module
  emitted each response

## 个人电脑 Codex 确定性修复

- Evidence bundle integrity verified: `no` (pending)
- Reproduced on exact source revision: `no` (pending)
- Root cause: (Codex to determine — hypothesis: response formatter selects
  locale from a per-turn detector without checking `session_state.language`
  first; the field is written on every locale change but never read at
  formatter dispatch)
- Violated architecture authority: (Codex to determine — likely a
  session-state contract violation)
- Single authoritative repair owner: (Codex to determine — likely
  `response_authority` or wherever the locale-of-response is chosen)
- Framework-level repair design: (Codex to determine)
- Files changed: (Codex to fill)
- Repair revision: (Codex to fill)
- Focused deterministic tests: (Codex to fill — must cover: session
  locale established by turn N, English input at turn N+1 → response in
  session locale; explicit locale-switch command → session field updated
  and subsequent responses honor new locale; fresh session on English input
  → session locale established as en, subsequent Chinese input → session
  locale respected)
- Adjacent regression tests: (Codex to fill — exit-command response locale
  behavior; multi-turn locale drift in long sessions)
- Complete Docker/Linux gates: (Codex to fill)
- Why this is not a phrase/keyword/transcript/terminal/verifier patch:
  (Codex to argue — hypothesized root cause is dispatch-layer session-state
  read, not any surface string or user-visible label)

## 工作电脑回验

- Clean checkout of repair revision: `no` (pending)
- Original failed journey result: pending (turn 5 must produce Chinese
  response after repair)
- Adjacent variant 1: fresh session in English, then Chinese input — Chinese
  input should honor session English locale until explicit switch
- Adjacent variant 2: session with explicit locale switch mid-conversation
  — must honor the new locale immediately
- Gemini/ADC/google_search result: N/A
- New evidence bundle + SHA-256: pending
- Remaining gap: pending
- Closure decision and reason: pending
