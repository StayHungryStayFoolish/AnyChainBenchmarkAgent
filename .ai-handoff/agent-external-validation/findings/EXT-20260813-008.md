# AnyChain Agent External Validation Record — EXT-20260813-008

Neither `restart` (bare) nor `/restart` (slash-prefixed) is recognized
as a command; both are rejected with the generic "clarify unmapped"
template. Users mid-config who want to abandon their partial state
and start fresh must type `exit` and re-launch the REPL from the
shell, or navigate all the way to a state where "N to change" is
offered. This blocks the manifest's `WW-RESTART-001` intent.

## 身份

- Record ID: `EXT-20260813-008`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Tracked worktree clean: `yes`
- Runtime: Ubuntu Docker `blockchain-node-benchmark:test-ubuntu`
  container `bench-preflight`
- Host: Debian 13 gLinux GCE VM, x86_64

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
- External user AI/model: Claude Sonnet 4.6 (Vertex `us-east5` rawPredict)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`

## 起始状态

- Manifest scenario ID: `WW-RESTART-001`
- Journey/task ID: fresh cold-start
- Session/checkpoint ID: fresh langgraph state
- Starting groups and states: mid-config after fake-node + ethereum
  binding
- Pending question ID/owner at turn 3: `Detected CLOUD_REGION:
  us-central1. Use this value? 1. Y  2. N`
- Chain case: `supported` (ethereum)
- Prior job ID/status: none

## 精确观察

- Previous complete agent response for turn 3:
  ```
  Agent> Confirmed chain: `ethereum`.
  Detected CLOUD_REGION: `us-central1`. Use this value?
  1. Y
  2. N
  Reply with an option number or name, or enter a custom value.
  ```
- Exact Sonnet user input for turn 3 (from `driver.jsonl`
  sha256=`1a23675a8479a8b14b67b9b9af65e17c99c67dec2e43596477434b269dd0e6ab`):
  `restart`
- Next complete agent response (from `session.log`
  sha256=`ef2a6953a44a8d5bcefd93f0e81163c0cb094d90b775109626b301d99adda3df`):
  ```
  Agent> [thinking] Processing the current request. Press Ctrl+C to cancel this turn without exiting the Agent.
  Agent> To avoid applying only part of this turn, clarify the following items that were not mapped safely:
  - restart
  Detected CLOUD_REGION: `us-central1`. Use this value?
  1. Y
  2. N
  Reply with an option number or name, or enter a custom value.
  ```
- Turn 4 `/restart` → identical generic clarify rejection
- Turn 5 `2` (N to CLOUD_REGION) accepted → agent proceeded to custom
  CLOUD_REGION prompt (this is the ONLY escape from a Y/N: not
  actually a restart, just a field decline)
- Turn 6 `exit` → `Agent> Exited AnyChain Benchmark Agent.`
- Tool receipt path + SHA-256: none — routing decision
- Runtime event/checkpoint path + SHA-256: `langgraph-snapshot.tgz`
  sha256=`13ee59ec8c0b9d1d682dc2828806f2aa4a7512f4e0414235999ba247c641992c`
- Session state path + SHA-256: `session-state.json`
  sha256=`43b0f0535ebc9c1a2841bf04d793ad494d0d7a97fc7f49acc8f30a63fcfcbc20`
- Driver log path + SHA-256: `driver.jsonl`
  sha256=`1a23675a8479a8b14b67b9b9af65e17c99c67dec2e43596477434b269dd0e6ab`
- First failing turn index: turn 3
- Expected contract/postcondition: at minimum ONE of `restart`,
  `/restart`, `/reset`, `reset session`, or a documented slash-command
  should trigger a clean session reset with confirmation ("You have
  partial config; discard and restart? Y/N"). The `help` starter hint
  (`Try: benchmark solana, use fake-node, doctor, jobs, status,
  logs <job_id>, follow <job_id>, help, exit.`) mentions no restart
  verb; if restart is genuinely not supported the hint should list
  the alternative ("to start over, `exit` and re-launch") or the
  agent should say so on the first rejection.
- Observed behavior: both attempted phrasings unmapped; no
  suggestion of the correct escape; user's only options are exit-and-
  relaunch or navigate the entire config flow
- User impact: users who realize mid-config they want to change chain
  or restart cannot cleanly do so from the REPL; they must exit and
  re-launch, losing any exploratory conversation state
- Reproduction count: 2 (`restart`, `/restart`)
- Fresh isolated session reproduced: yes

## 分类

- Classification: `product_failure`
- Suspected root owner: `semantic_planning` (session-control intent
  routing) or `documentation` (missing user education about how to
  restart)
- Supporting evidence: same generic-clarify pattern seen in
  EXT-20260813-001/003/004/005/006/007 — all point to a single
  strict-match gate with no session-control verb recognition
- Evidence against stale session/data contamination: fresh session
- Not tested because: (i) `/reset`, `reset`, `start over`, `new
  session`, `abort` variations not tested; (ii) the `exit + re-enter`
  path was blocked by the driver's process-per-scenario model —
  Codex should test this

## 安全传输

- Redaction review completed by: Claude Sonnet 4.6 (recorder)
- Bundle path: `.agent/external-validation/a22108e53401cf7d9314917b870b71ccf338619c/transcripts/WW-RESTART-001/`
- Bundle SHA-256: per-file hashes in updated SHA256SUMS
- Internal `SHA256SUMS` verified on personal workstation: `no`
- Missing evidence: post-job restart state (no job existed in this
  session — would need EXEC-001-style submission first, then
  restart mid-job)

## 个人电脑 Codex 确定性修复

- Evidence bundle integrity verified: pending Codex
- Reproduced on exact source revision: pending Codex
- Root cause: pending Codex — hypothesis: no restart intent handler
  exists; the starter hint doesn't list a restart verb
- Violated architecture authority: pending Codex — likely a "session
  control" contract that should exist
- Single authoritative repair owner: `semantic_planning` most likely
- Framework-level repair design: pending Codex — add an explicit
  `restart`, `/restart`, `reset` command that offers Y/N confirm and
  wipes the current turn context. Update the starter hint to include
  it. Document explicitly.
- Files changed: pending Codex
- Repair revision: pending Codex
- Focused deterministic tests: `restart`, `/restart`, `/reset`,
  `reset`, `start over` at various mid-config prompts
- Adjacent regression tests: `exit` still exits cleanly; `Y`/`N`
  still work; option numbers still work
- Complete Docker/Linux gates: pending Codex
- Why this is not a phrase/keyword patch: the fix must add a real
  session-control intent with confirm-Y/N, not a string-match on
  the phrasings tested

## 工作电脑回验

- Clean checkout of repair revision: pending
- Original failed journey result: pending
- Adjacent variant 1: `/reset` — pending
- Adjacent variant 2: `start over` — pending
- Gemini/ADC/google_search result: pending
- New evidence bundle + SHA-256: pending
- Remaining gap: pending
- Closure decision and reason: pending
