# AnyChain Agent External Validation Record — EXT-20260813-004

Multi-field key=value env-style input
(`chain=ethereum, qps=500, mode=fake-node, duration=60s`) is rejected
with the generic "clarify unmapped" template. The agent does not
extract the recognizable keys and does not confirm field-by-field.
This is a direct failure of the manifest scenario `WW-MULTI-001` which
targets exactly this ergonomic pathway.

## 身份

- Record ID: `EXT-20260813-004`
- Finding present: `yes`
- Status: `observed_failure`
- Acceptance class: `discovery`
- Source branch: `docs/agent-handoff-chaos`
- Full source revision: `a22108e53401cf7d9314917b870b71ccf338619c`
- Tracked worktree clean: `yes`
- Runtime: Ubuntu Docker `blockchain-node-benchmark:test-ubuntu`
  container `bench-preflight`
- Host: Debian 13 gLinux GCE VM, x86_64

## AI 与认证

- Agent provider/model/auth: `claude / claude-sonnet-4-6 / google_adc`
  (Leland-authorized discovery substitution)
- External user AI/model: Claude Sonnet 4.6 (Vertex `us-east5` rawPredict,
  discovery substitution for manifest role `gemini_via_vertex_adc`)
- External user role: `response_driven_discovery`
- ADC project/location: `claude-ttft-test / us-east5`
- Preflight evidence hashes: see PASS-WW-STARTUP-001-rerun

## 起始状态

- Manifest scenario ID: `WW-MULTI-001`
- Journey/task ID: fresh cold-start, no prior job
- Session/checkpoint ID: fresh langgraph state
- Starting groups and states: initial banner + orientation menu
- Pending question ID/owner: orientation menu at turn 1
- Chain case: `supported` (ethereum embedded in the multi-key input)
- Prior job ID/status: none

## 精确观察

- Previous complete agent response for turn 1: initial orientation
  menu (see `session.log` sha256=
  `73a1d78970a7c569e244b7ffe580ce9a9c1cadd002e27a00468d12a30f862cbf`)
- Exact Sonnet user input for turn 1 (from `driver.jsonl`
  sha256=`ea90e9b77e0655611584e3ae936d017b671f578cf2c5536a4fb878949eee7bff`):
  `chain=ethereum, qps=500, mode=fake-node, duration=60s`
- Next complete agent response (turn 1):
  ```
  Agent> [thinking] Processing the current request. Press Ctrl+C to cancel this turn without exiting the Agent.
  Agent> To avoid applying only part of this turn, clarify the following items that were not mapped safely:
  - chain=ethereum, qps=500, mode=fake-node, duration=60s
  I am AnyChain Benchmark Agent. What would you like help with? State a test goal or choose the next step.
  [orientation menu re-emitted]
  ```
- Additional failure: turn 3 same input attempted AGAIN at pending
  "Which chain do you want to benchmark?" — same generic rejection
- Contrast: turn 4 `ethereum` (bare token) → accepted
- Tool receipt path + SHA-256: none — routing/parsing decision
- Runtime event/checkpoint path + SHA-256: `langgraph-snapshot.tgz`
  sha256=`6cc5eede6bf108d2d52769f063f1094480b4e4c47dd30bc18cd283ce3a6d089b`
- Session state path + SHA-256: `session-state.json`
  sha256=`43b0f0535ebc9c1a2841bf04d793ad494d0d7a97fc7f49acc8f30a63fcfcbc20`
- Driver log path + SHA-256: `driver.jsonl`
  sha256=`ea90e9b77e0655611584e3ae936d017b671f578cf2c5536a4fb878949eee7bff`
- First failing turn index: turn 1
- Expected contract/postcondition: per the manifest scenario objective,
  the agent should accept the multi-field submission and confirm
  field-by-field ("Confirmed chain: `ethereum`. Confirmed target mode:
  `fake-node`. Now confirming QPS = 500 — is this correct? Y/N").
  Even a subset ("I recognized `chain=ethereum` and `mode=fake-node`;
  what should we do with `qps=500` and `duration=60s`?") would be
  acceptable.
- Observed behavior: the entire multi-key string is echoed verbatim in
  the generic "not mapped safely" template. No key extraction is
  attempted. This same pattern is used for chatty sentences
  (EXT-20260813-003), Chinese affirmatives (EXT-20260813-002), and
  language-switch attempts (EXT-20260813-001) — pointing to a single
  strict-match gate with no NL/structured-input handling.
- User impact: users familiar with `env=value` conventions from CLI
  tools (Docker, k8s, most CI systems) will type this out of habit
  and be blocked. The agent gives no hint that env-style is
  unsupported nor lists what IS supported (only bare tokens / option
  numbers / free values).
- Reproduction count: 2 (turn 1 at orientation, turn 3 at chain prompt)
- Fresh isolated session reproduced: yes

## 分类

- Classification: `product_failure`
- Suspected root owner: `semantic_planning` (multi-field intent
  extraction)
- Supporting evidence: identical patterns in EXT-20260813-001, -002,
  -003 all bucket into the same clarify template; single strict-match
  gate is the shared cause
- Evidence against stale session/data contamination: fresh session
- Not tested because: (i) YAML block input not tested (this session
  ran out of turns after multi-key rejection); (ii) JSON body input
  not tested; (iii) prose input like "please benchmark ethereum with
  500 qps for 60 seconds" not tested

## 安全传输

- Redaction review completed by: Claude Sonnet 4.6 (recorder)
- Bundle path: `.agent/external-validation/a22108e53401cf7d9314917b870b71ccf338619c/transcripts/WW-MULTI-001/`
- Bundle SHA-256: per-file hashes in updated SHA256SUMS
- Internal `SHA256SUMS` verified on personal workstation: `no`
- Missing evidence: YAML / JSON / prose variants not tested (turn
  budget exhausted after key=value rejection)

## 个人电脑 Codex 确定性修复

- Evidence bundle integrity verified: pending Codex
- Reproduced on exact source revision: pending Codex
- Root cause: pending Codex — hypothesis: no key=value parser exists
  in the pending-question dispatcher; the surface is strictly single-
  answer (option number OR exact option name OR literal free-value)
- Violated architecture authority: pending Codex — likely the
  "multi-field ergonomic" contract implied by the manifest scenario
- Single authoritative repair owner: `semantic_planning`
- Framework-level repair design: pending Codex — either (a) add a
  multi-field parser (key=value, YAML, JSON) that stages values and
  confirms field-by-field, or (b) add an explicit "one field per turn;
  use `chain=ethereum` after entering the config flow to preseed" help
  message when multi-key input is detected
- Files changed: pending Codex
- Repair revision: pending Codex
- Focused deterministic tests: key=value string with 2/3/4/5 keys, one
  invalid key, unknown chain value in the key, YAML block, JSON body,
  prose with two keys
- Adjacent regression tests: bare token acceptance still works; option
  numbers still work; free-values (`3000`, `pd-ssd`) still work
- Complete Docker/Linux gates: pending Codex
- Why this is not a phrase/keyword patch: the fix must be a real
  parser or explicit educational response — not string matches on
  the specific test input

## 工作电脑回验

- Clean checkout of repair revision: pending
- Original failed journey result: pending
- Adjacent variant 1: YAML `chain: ethereum\nqps: 500` — pending
- Adjacent variant 2: JSON `{"chain":"ethereum","qps":500}` — pending
- Gemini/ADC/google_search result: pending (Claude substitution run)
- New evidence bundle + SHA-256: pending
- Remaining gap: pending
- Closure decision and reason: pending
